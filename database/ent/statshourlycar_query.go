// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jianbo-zh/jydata/database/ent/car"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"github.com/jianbo-zh/jydata/database/ent/scenicarea"
	"github.com/jianbo-zh/jydata/database/ent/statshourlycar"
)

// StatsHourlyCarQuery is the builder for querying StatsHourlyCar entities.
type StatsHourlyCarQuery struct {
	config
	ctx                      *QueryContext
	order                    []statshourlycar.OrderOption
	inters                   []Interceptor
	predicates               []predicate.StatsHourlyCar
	withBackgroundScenicArea *ScenicAreaQuery
	withCar                  *CarQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StatsHourlyCarQuery builder.
func (shcq *StatsHourlyCarQuery) Where(ps ...predicate.StatsHourlyCar) *StatsHourlyCarQuery {
	shcq.predicates = append(shcq.predicates, ps...)
	return shcq
}

// Limit the number of records to be returned by this query.
func (shcq *StatsHourlyCarQuery) Limit(limit int) *StatsHourlyCarQuery {
	shcq.ctx.Limit = &limit
	return shcq
}

// Offset to start from.
func (shcq *StatsHourlyCarQuery) Offset(offset int) *StatsHourlyCarQuery {
	shcq.ctx.Offset = &offset
	return shcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (shcq *StatsHourlyCarQuery) Unique(unique bool) *StatsHourlyCarQuery {
	shcq.ctx.Unique = &unique
	return shcq
}

// Order specifies how the records should be ordered.
func (shcq *StatsHourlyCarQuery) Order(o ...statshourlycar.OrderOption) *StatsHourlyCarQuery {
	shcq.order = append(shcq.order, o...)
	return shcq
}

// QueryBackgroundScenicArea chains the current query on the "background_scenic_area" edge.
func (shcq *StatsHourlyCarQuery) QueryBackgroundScenicArea() *ScenicAreaQuery {
	query := (&ScenicAreaClient{config: shcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := shcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := shcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(statshourlycar.Table, statshourlycar.FieldID, selector),
			sqlgraph.To(scenicarea.Table, scenicarea.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, statshourlycar.BackgroundScenicAreaTable, statshourlycar.BackgroundScenicAreaColumn),
		)
		fromU = sqlgraph.SetNeighbors(shcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCar chains the current query on the "car" edge.
func (shcq *StatsHourlyCarQuery) QueryCar() *CarQuery {
	query := (&CarClient{config: shcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := shcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := shcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(statshourlycar.Table, statshourlycar.FieldID, selector),
			sqlgraph.To(car.Table, car.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, statshourlycar.CarTable, statshourlycar.CarColumn),
		)
		fromU = sqlgraph.SetNeighbors(shcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first StatsHourlyCar entity from the query.
// Returns a *NotFoundError when no StatsHourlyCar was found.
func (shcq *StatsHourlyCarQuery) First(ctx context.Context) (*StatsHourlyCar, error) {
	nodes, err := shcq.Limit(1).All(setContextOp(ctx, shcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{statshourlycar.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (shcq *StatsHourlyCarQuery) FirstX(ctx context.Context) *StatsHourlyCar {
	node, err := shcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StatsHourlyCar ID from the query.
// Returns a *NotFoundError when no StatsHourlyCar ID was found.
func (shcq *StatsHourlyCarQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = shcq.Limit(1).IDs(setContextOp(ctx, shcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{statshourlycar.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (shcq *StatsHourlyCarQuery) FirstIDX(ctx context.Context) int {
	id, err := shcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StatsHourlyCar entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StatsHourlyCar entity is found.
// Returns a *NotFoundError when no StatsHourlyCar entities are found.
func (shcq *StatsHourlyCarQuery) Only(ctx context.Context) (*StatsHourlyCar, error) {
	nodes, err := shcq.Limit(2).All(setContextOp(ctx, shcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{statshourlycar.Label}
	default:
		return nil, &NotSingularError{statshourlycar.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (shcq *StatsHourlyCarQuery) OnlyX(ctx context.Context) *StatsHourlyCar {
	node, err := shcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StatsHourlyCar ID in the query.
// Returns a *NotSingularError when more than one StatsHourlyCar ID is found.
// Returns a *NotFoundError when no entities are found.
func (shcq *StatsHourlyCarQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = shcq.Limit(2).IDs(setContextOp(ctx, shcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{statshourlycar.Label}
	default:
		err = &NotSingularError{statshourlycar.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (shcq *StatsHourlyCarQuery) OnlyIDX(ctx context.Context) int {
	id, err := shcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StatsHourlyCars.
func (shcq *StatsHourlyCarQuery) All(ctx context.Context) ([]*StatsHourlyCar, error) {
	ctx = setContextOp(ctx, shcq.ctx, ent.OpQueryAll)
	if err := shcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StatsHourlyCar, *StatsHourlyCarQuery]()
	return withInterceptors[[]*StatsHourlyCar](ctx, shcq, qr, shcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (shcq *StatsHourlyCarQuery) AllX(ctx context.Context) []*StatsHourlyCar {
	nodes, err := shcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StatsHourlyCar IDs.
func (shcq *StatsHourlyCarQuery) IDs(ctx context.Context) (ids []int, err error) {
	if shcq.ctx.Unique == nil && shcq.path != nil {
		shcq.Unique(true)
	}
	ctx = setContextOp(ctx, shcq.ctx, ent.OpQueryIDs)
	if err = shcq.Select(statshourlycar.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (shcq *StatsHourlyCarQuery) IDsX(ctx context.Context) []int {
	ids, err := shcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (shcq *StatsHourlyCarQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, shcq.ctx, ent.OpQueryCount)
	if err := shcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, shcq, querierCount[*StatsHourlyCarQuery](), shcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (shcq *StatsHourlyCarQuery) CountX(ctx context.Context) int {
	count, err := shcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (shcq *StatsHourlyCarQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, shcq.ctx, ent.OpQueryExist)
	switch _, err := shcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (shcq *StatsHourlyCarQuery) ExistX(ctx context.Context) bool {
	exist, err := shcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StatsHourlyCarQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (shcq *StatsHourlyCarQuery) Clone() *StatsHourlyCarQuery {
	if shcq == nil {
		return nil
	}
	return &StatsHourlyCarQuery{
		config:                   shcq.config,
		ctx:                      shcq.ctx.Clone(),
		order:                    append([]statshourlycar.OrderOption{}, shcq.order...),
		inters:                   append([]Interceptor{}, shcq.inters...),
		predicates:               append([]predicate.StatsHourlyCar{}, shcq.predicates...),
		withBackgroundScenicArea: shcq.withBackgroundScenicArea.Clone(),
		withCar:                  shcq.withCar.Clone(),
		// clone intermediate query.
		sql:  shcq.sql.Clone(),
		path: shcq.path,
	}
}

// WithBackgroundScenicArea tells the query-builder to eager-load the nodes that are connected to
// the "background_scenic_area" edge. The optional arguments are used to configure the query builder of the edge.
func (shcq *StatsHourlyCarQuery) WithBackgroundScenicArea(opts ...func(*ScenicAreaQuery)) *StatsHourlyCarQuery {
	query := (&ScenicAreaClient{config: shcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	shcq.withBackgroundScenicArea = query
	return shcq
}

// WithCar tells the query-builder to eager-load the nodes that are connected to
// the "car" edge. The optional arguments are used to configure the query builder of the edge.
func (shcq *StatsHourlyCarQuery) WithCar(opts ...func(*CarQuery)) *StatsHourlyCarQuery {
	query := (&CarClient{config: shcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	shcq.withCar = query
	return shcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StatsHourlyCar.Query().
//		GroupBy(statshourlycar.FieldScenicAreaID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (shcq *StatsHourlyCarQuery) GroupBy(field string, fields ...string) *StatsHourlyCarGroupBy {
	shcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StatsHourlyCarGroupBy{build: shcq}
	grbuild.flds = &shcq.ctx.Fields
	grbuild.label = statshourlycar.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//	}
//
//	client.StatsHourlyCar.Query().
//		Select(statshourlycar.FieldScenicAreaID).
//		Scan(ctx, &v)
func (shcq *StatsHourlyCarQuery) Select(fields ...string) *StatsHourlyCarSelect {
	shcq.ctx.Fields = append(shcq.ctx.Fields, fields...)
	sbuild := &StatsHourlyCarSelect{StatsHourlyCarQuery: shcq}
	sbuild.label = statshourlycar.Label
	sbuild.flds, sbuild.scan = &shcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StatsHourlyCarSelect configured with the given aggregations.
func (shcq *StatsHourlyCarQuery) Aggregate(fns ...AggregateFunc) *StatsHourlyCarSelect {
	return shcq.Select().Aggregate(fns...)
}

func (shcq *StatsHourlyCarQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range shcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, shcq); err != nil {
				return err
			}
		}
	}
	for _, f := range shcq.ctx.Fields {
		if !statshourlycar.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if shcq.path != nil {
		prev, err := shcq.path(ctx)
		if err != nil {
			return err
		}
		shcq.sql = prev
	}
	return nil
}

func (shcq *StatsHourlyCarQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StatsHourlyCar, error) {
	var (
		nodes       = []*StatsHourlyCar{}
		_spec       = shcq.querySpec()
		loadedTypes = [2]bool{
			shcq.withBackgroundScenicArea != nil,
			shcq.withCar != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StatsHourlyCar).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StatsHourlyCar{config: shcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, shcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := shcq.withBackgroundScenicArea; query != nil {
		if err := shcq.loadBackgroundScenicArea(ctx, query, nodes, nil,
			func(n *StatsHourlyCar, e *ScenicArea) { n.Edges.BackgroundScenicArea = e }); err != nil {
			return nil, err
		}
	}
	if query := shcq.withCar; query != nil {
		if err := shcq.loadCar(ctx, query, nodes, nil,
			func(n *StatsHourlyCar, e *Car) { n.Edges.Car = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (shcq *StatsHourlyCarQuery) loadBackgroundScenicArea(ctx context.Context, query *ScenicAreaQuery, nodes []*StatsHourlyCar, init func(*StatsHourlyCar), assign func(*StatsHourlyCar, *ScenicArea)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*StatsHourlyCar)
	for i := range nodes {
		fk := nodes[i].ScenicAreaID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(scenicarea.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "scenic_area_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (shcq *StatsHourlyCarQuery) loadCar(ctx context.Context, query *CarQuery, nodes []*StatsHourlyCar, init func(*StatsHourlyCar), assign func(*StatsHourlyCar, *Car)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*StatsHourlyCar)
	for i := range nodes {
		fk := nodes[i].CarID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(car.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "car_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (shcq *StatsHourlyCarQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := shcq.querySpec()
	_spec.Node.Columns = shcq.ctx.Fields
	if len(shcq.ctx.Fields) > 0 {
		_spec.Unique = shcq.ctx.Unique != nil && *shcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, shcq.driver, _spec)
}

func (shcq *StatsHourlyCarQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(statshourlycar.Table, statshourlycar.Columns, sqlgraph.NewFieldSpec(statshourlycar.FieldID, field.TypeInt))
	_spec.From = shcq.sql
	if unique := shcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if shcq.path != nil {
		_spec.Unique = true
	}
	if fields := shcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, statshourlycar.FieldID)
		for i := range fields {
			if fields[i] != statshourlycar.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if shcq.withBackgroundScenicArea != nil {
			_spec.Node.AddColumnOnce(statshourlycar.FieldScenicAreaID)
		}
		if shcq.withCar != nil {
			_spec.Node.AddColumnOnce(statshourlycar.FieldCarID)
		}
	}
	if ps := shcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := shcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := shcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := shcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (shcq *StatsHourlyCarQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(shcq.driver.Dialect())
	t1 := builder.Table(statshourlycar.Table)
	columns := shcq.ctx.Fields
	if len(columns) == 0 {
		columns = statshourlycar.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if shcq.sql != nil {
		selector = shcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if shcq.ctx.Unique != nil && *shcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range shcq.predicates {
		p(selector)
	}
	for _, p := range shcq.order {
		p(selector)
	}
	if offset := shcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := shcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StatsHourlyCarGroupBy is the group-by builder for StatsHourlyCar entities.
type StatsHourlyCarGroupBy struct {
	selector
	build *StatsHourlyCarQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (shcgb *StatsHourlyCarGroupBy) Aggregate(fns ...AggregateFunc) *StatsHourlyCarGroupBy {
	shcgb.fns = append(shcgb.fns, fns...)
	return shcgb
}

// Scan applies the selector query and scans the result into the given value.
func (shcgb *StatsHourlyCarGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, shcgb.build.ctx, ent.OpQueryGroupBy)
	if err := shcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsHourlyCarQuery, *StatsHourlyCarGroupBy](ctx, shcgb.build, shcgb, shcgb.build.inters, v)
}

func (shcgb *StatsHourlyCarGroupBy) sqlScan(ctx context.Context, root *StatsHourlyCarQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(shcgb.fns))
	for _, fn := range shcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*shcgb.flds)+len(shcgb.fns))
		for _, f := range *shcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*shcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := shcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StatsHourlyCarSelect is the builder for selecting fields of StatsHourlyCar entities.
type StatsHourlyCarSelect struct {
	*StatsHourlyCarQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (shcs *StatsHourlyCarSelect) Aggregate(fns ...AggregateFunc) *StatsHourlyCarSelect {
	shcs.fns = append(shcs.fns, fns...)
	return shcs
}

// Scan applies the selector query and scans the result into the given value.
func (shcs *StatsHourlyCarSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, shcs.ctx, ent.OpQuerySelect)
	if err := shcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsHourlyCarQuery, *StatsHourlyCarSelect](ctx, shcs.StatsHourlyCarQuery, shcs, shcs.inters, v)
}

func (shcs *StatsHourlyCarSelect) sqlScan(ctx context.Context, root *StatsHourlyCarQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(shcs.fns))
	for _, fn := range shcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*shcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := shcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
