// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jianbo-zh/jydata/database/ent/carconfigpack"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
)

// CarConfigPackQuery is the builder for querying CarConfigPack entities.
type CarConfigPackQuery struct {
	config
	ctx        *QueryContext
	order      []carconfigpack.OrderOption
	inters     []Interceptor
	predicates []predicate.CarConfigPack
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarConfigPackQuery builder.
func (ccpq *CarConfigPackQuery) Where(ps ...predicate.CarConfigPack) *CarConfigPackQuery {
	ccpq.predicates = append(ccpq.predicates, ps...)
	return ccpq
}

// Limit the number of records to be returned by this query.
func (ccpq *CarConfigPackQuery) Limit(limit int) *CarConfigPackQuery {
	ccpq.ctx.Limit = &limit
	return ccpq
}

// Offset to start from.
func (ccpq *CarConfigPackQuery) Offset(offset int) *CarConfigPackQuery {
	ccpq.ctx.Offset = &offset
	return ccpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ccpq *CarConfigPackQuery) Unique(unique bool) *CarConfigPackQuery {
	ccpq.ctx.Unique = &unique
	return ccpq
}

// Order specifies how the records should be ordered.
func (ccpq *CarConfigPackQuery) Order(o ...carconfigpack.OrderOption) *CarConfigPackQuery {
	ccpq.order = append(ccpq.order, o...)
	return ccpq
}

// First returns the first CarConfigPack entity from the query.
// Returns a *NotFoundError when no CarConfigPack was found.
func (ccpq *CarConfigPackQuery) First(ctx context.Context) (*CarConfigPack, error) {
	nodes, err := ccpq.Limit(1).All(setContextOp(ctx, ccpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carconfigpack.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ccpq *CarConfigPackQuery) FirstX(ctx context.Context) *CarConfigPack {
	node, err := ccpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarConfigPack ID from the query.
// Returns a *NotFoundError when no CarConfigPack ID was found.
func (ccpq *CarConfigPackQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ccpq.Limit(1).IDs(setContextOp(ctx, ccpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carconfigpack.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ccpq *CarConfigPackQuery) FirstIDX(ctx context.Context) int {
	id, err := ccpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarConfigPack entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarConfigPack entity is found.
// Returns a *NotFoundError when no CarConfigPack entities are found.
func (ccpq *CarConfigPackQuery) Only(ctx context.Context) (*CarConfigPack, error) {
	nodes, err := ccpq.Limit(2).All(setContextOp(ctx, ccpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carconfigpack.Label}
	default:
		return nil, &NotSingularError{carconfigpack.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ccpq *CarConfigPackQuery) OnlyX(ctx context.Context) *CarConfigPack {
	node, err := ccpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarConfigPack ID in the query.
// Returns a *NotSingularError when more than one CarConfigPack ID is found.
// Returns a *NotFoundError when no entities are found.
func (ccpq *CarConfigPackQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ccpq.Limit(2).IDs(setContextOp(ctx, ccpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carconfigpack.Label}
	default:
		err = &NotSingularError{carconfigpack.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ccpq *CarConfigPackQuery) OnlyIDX(ctx context.Context) int {
	id, err := ccpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarConfigPacks.
func (ccpq *CarConfigPackQuery) All(ctx context.Context) ([]*CarConfigPack, error) {
	ctx = setContextOp(ctx, ccpq.ctx, ent.OpQueryAll)
	if err := ccpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarConfigPack, *CarConfigPackQuery]()
	return withInterceptors[[]*CarConfigPack](ctx, ccpq, qr, ccpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ccpq *CarConfigPackQuery) AllX(ctx context.Context) []*CarConfigPack {
	nodes, err := ccpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarConfigPack IDs.
func (ccpq *CarConfigPackQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ccpq.ctx.Unique == nil && ccpq.path != nil {
		ccpq.Unique(true)
	}
	ctx = setContextOp(ctx, ccpq.ctx, ent.OpQueryIDs)
	if err = ccpq.Select(carconfigpack.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ccpq *CarConfigPackQuery) IDsX(ctx context.Context) []int {
	ids, err := ccpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ccpq *CarConfigPackQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ccpq.ctx, ent.OpQueryCount)
	if err := ccpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ccpq, querierCount[*CarConfigPackQuery](), ccpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ccpq *CarConfigPackQuery) CountX(ctx context.Context) int {
	count, err := ccpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ccpq *CarConfigPackQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ccpq.ctx, ent.OpQueryExist)
	switch _, err := ccpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ccpq *CarConfigPackQuery) ExistX(ctx context.Context) bool {
	exist, err := ccpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarConfigPackQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ccpq *CarConfigPackQuery) Clone() *CarConfigPackQuery {
	if ccpq == nil {
		return nil
	}
	return &CarConfigPackQuery{
		config:     ccpq.config,
		ctx:        ccpq.ctx.Clone(),
		order:      append([]carconfigpack.OrderOption{}, ccpq.order...),
		inters:     append([]Interceptor{}, ccpq.inters...),
		predicates: append([]predicate.CarConfigPack{}, ccpq.predicates...),
		// clone intermediate query.
		sql:  ccpq.sql.Clone(),
		path: ccpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarConfigPack.Query().
//		GroupBy(carconfigpack.FieldScenicAreaID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ccpq *CarConfigPackQuery) GroupBy(field string, fields ...string) *CarConfigPackGroupBy {
	ccpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarConfigPackGroupBy{build: ccpq}
	grbuild.flds = &ccpq.ctx.Fields
	grbuild.label = carconfigpack.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//	}
//
//	client.CarConfigPack.Query().
//		Select(carconfigpack.FieldScenicAreaID).
//		Scan(ctx, &v)
func (ccpq *CarConfigPackQuery) Select(fields ...string) *CarConfigPackSelect {
	ccpq.ctx.Fields = append(ccpq.ctx.Fields, fields...)
	sbuild := &CarConfigPackSelect{CarConfigPackQuery: ccpq}
	sbuild.label = carconfigpack.Label
	sbuild.flds, sbuild.scan = &ccpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarConfigPackSelect configured with the given aggregations.
func (ccpq *CarConfigPackQuery) Aggregate(fns ...AggregateFunc) *CarConfigPackSelect {
	return ccpq.Select().Aggregate(fns...)
}

func (ccpq *CarConfigPackQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ccpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ccpq); err != nil {
				return err
			}
		}
	}
	for _, f := range ccpq.ctx.Fields {
		if !carconfigpack.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ccpq.path != nil {
		prev, err := ccpq.path(ctx)
		if err != nil {
			return err
		}
		ccpq.sql = prev
	}
	return nil
}

func (ccpq *CarConfigPackQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarConfigPack, error) {
	var (
		nodes = []*CarConfigPack{}
		_spec = ccpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarConfigPack).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarConfigPack{config: ccpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ccpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ccpq *CarConfigPackQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ccpq.querySpec()
	_spec.Node.Columns = ccpq.ctx.Fields
	if len(ccpq.ctx.Fields) > 0 {
		_spec.Unique = ccpq.ctx.Unique != nil && *ccpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ccpq.driver, _spec)
}

func (ccpq *CarConfigPackQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carconfigpack.Table, carconfigpack.Columns, sqlgraph.NewFieldSpec(carconfigpack.FieldID, field.TypeInt))
	_spec.From = ccpq.sql
	if unique := ccpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ccpq.path != nil {
		_spec.Unique = true
	}
	if fields := ccpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carconfigpack.FieldID)
		for i := range fields {
			if fields[i] != carconfigpack.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ccpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ccpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ccpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ccpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ccpq *CarConfigPackQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ccpq.driver.Dialect())
	t1 := builder.Table(carconfigpack.Table)
	columns := ccpq.ctx.Fields
	if len(columns) == 0 {
		columns = carconfigpack.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ccpq.sql != nil {
		selector = ccpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ccpq.ctx.Unique != nil && *ccpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ccpq.predicates {
		p(selector)
	}
	for _, p := range ccpq.order {
		p(selector)
	}
	if offset := ccpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ccpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CarConfigPackGroupBy is the group-by builder for CarConfigPack entities.
type CarConfigPackGroupBy struct {
	selector
	build *CarConfigPackQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ccpgb *CarConfigPackGroupBy) Aggregate(fns ...AggregateFunc) *CarConfigPackGroupBy {
	ccpgb.fns = append(ccpgb.fns, fns...)
	return ccpgb
}

// Scan applies the selector query and scans the result into the given value.
func (ccpgb *CarConfigPackGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccpgb.build.ctx, ent.OpQueryGroupBy)
	if err := ccpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarConfigPackQuery, *CarConfigPackGroupBy](ctx, ccpgb.build, ccpgb, ccpgb.build.inters, v)
}

func (ccpgb *CarConfigPackGroupBy) sqlScan(ctx context.Context, root *CarConfigPackQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ccpgb.fns))
	for _, fn := range ccpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ccpgb.flds)+len(ccpgb.fns))
		for _, f := range *ccpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ccpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarConfigPackSelect is the builder for selecting fields of CarConfigPack entities.
type CarConfigPackSelect struct {
	*CarConfigPackQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ccps *CarConfigPackSelect) Aggregate(fns ...AggregateFunc) *CarConfigPackSelect {
	ccps.fns = append(ccps.fns, fns...)
	return ccps
}

// Scan applies the selector query and scans the result into the given value.
func (ccps *CarConfigPackSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccps.ctx, ent.OpQuerySelect)
	if err := ccps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarConfigPackQuery, *CarConfigPackSelect](ctx, ccps.CarConfigPackQuery, ccps, ccps.inters, v)
}

func (ccps *CarConfigPackSelect) sqlScan(ctx context.Context, root *CarConfigPackQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ccps.fns))
	for _, fn := range ccps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ccps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
