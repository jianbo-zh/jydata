// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jianbo-zh/jydata/database/ent/carconfigstatus"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
)

// CarConfigStatusQuery is the builder for querying CarConfigStatus entities.
type CarConfigStatusQuery struct {
	config
	ctx        *QueryContext
	order      []carconfigstatus.OrderOption
	inters     []Interceptor
	predicates []predicate.CarConfigStatus
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarConfigStatusQuery builder.
func (ccsq *CarConfigStatusQuery) Where(ps ...predicate.CarConfigStatus) *CarConfigStatusQuery {
	ccsq.predicates = append(ccsq.predicates, ps...)
	return ccsq
}

// Limit the number of records to be returned by this query.
func (ccsq *CarConfigStatusQuery) Limit(limit int) *CarConfigStatusQuery {
	ccsq.ctx.Limit = &limit
	return ccsq
}

// Offset to start from.
func (ccsq *CarConfigStatusQuery) Offset(offset int) *CarConfigStatusQuery {
	ccsq.ctx.Offset = &offset
	return ccsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ccsq *CarConfigStatusQuery) Unique(unique bool) *CarConfigStatusQuery {
	ccsq.ctx.Unique = &unique
	return ccsq
}

// Order specifies how the records should be ordered.
func (ccsq *CarConfigStatusQuery) Order(o ...carconfigstatus.OrderOption) *CarConfigStatusQuery {
	ccsq.order = append(ccsq.order, o...)
	return ccsq
}

// First returns the first CarConfigStatus entity from the query.
// Returns a *NotFoundError when no CarConfigStatus was found.
func (ccsq *CarConfigStatusQuery) First(ctx context.Context) (*CarConfigStatus, error) {
	nodes, err := ccsq.Limit(1).All(setContextOp(ctx, ccsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carconfigstatus.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ccsq *CarConfigStatusQuery) FirstX(ctx context.Context) *CarConfigStatus {
	node, err := ccsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarConfigStatus ID from the query.
// Returns a *NotFoundError when no CarConfigStatus ID was found.
func (ccsq *CarConfigStatusQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ccsq.Limit(1).IDs(setContextOp(ctx, ccsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carconfigstatus.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ccsq *CarConfigStatusQuery) FirstIDX(ctx context.Context) int {
	id, err := ccsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarConfigStatus entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarConfigStatus entity is found.
// Returns a *NotFoundError when no CarConfigStatus entities are found.
func (ccsq *CarConfigStatusQuery) Only(ctx context.Context) (*CarConfigStatus, error) {
	nodes, err := ccsq.Limit(2).All(setContextOp(ctx, ccsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carconfigstatus.Label}
	default:
		return nil, &NotSingularError{carconfigstatus.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ccsq *CarConfigStatusQuery) OnlyX(ctx context.Context) *CarConfigStatus {
	node, err := ccsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarConfigStatus ID in the query.
// Returns a *NotSingularError when more than one CarConfigStatus ID is found.
// Returns a *NotFoundError when no entities are found.
func (ccsq *CarConfigStatusQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ccsq.Limit(2).IDs(setContextOp(ctx, ccsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carconfigstatus.Label}
	default:
		err = &NotSingularError{carconfigstatus.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ccsq *CarConfigStatusQuery) OnlyIDX(ctx context.Context) int {
	id, err := ccsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarConfigStatusSlice.
func (ccsq *CarConfigStatusQuery) All(ctx context.Context) ([]*CarConfigStatus, error) {
	ctx = setContextOp(ctx, ccsq.ctx, ent.OpQueryAll)
	if err := ccsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarConfigStatus, *CarConfigStatusQuery]()
	return withInterceptors[[]*CarConfigStatus](ctx, ccsq, qr, ccsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ccsq *CarConfigStatusQuery) AllX(ctx context.Context) []*CarConfigStatus {
	nodes, err := ccsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarConfigStatus IDs.
func (ccsq *CarConfigStatusQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ccsq.ctx.Unique == nil && ccsq.path != nil {
		ccsq.Unique(true)
	}
	ctx = setContextOp(ctx, ccsq.ctx, ent.OpQueryIDs)
	if err = ccsq.Select(carconfigstatus.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ccsq *CarConfigStatusQuery) IDsX(ctx context.Context) []int {
	ids, err := ccsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ccsq *CarConfigStatusQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ccsq.ctx, ent.OpQueryCount)
	if err := ccsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ccsq, querierCount[*CarConfigStatusQuery](), ccsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ccsq *CarConfigStatusQuery) CountX(ctx context.Context) int {
	count, err := ccsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ccsq *CarConfigStatusQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ccsq.ctx, ent.OpQueryExist)
	switch _, err := ccsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ccsq *CarConfigStatusQuery) ExistX(ctx context.Context) bool {
	exist, err := ccsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarConfigStatusQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ccsq *CarConfigStatusQuery) Clone() *CarConfigStatusQuery {
	if ccsq == nil {
		return nil
	}
	return &CarConfigStatusQuery{
		config:     ccsq.config,
		ctx:        ccsq.ctx.Clone(),
		order:      append([]carconfigstatus.OrderOption{}, ccsq.order...),
		inters:     append([]Interceptor{}, ccsq.inters...),
		predicates: append([]predicate.CarConfigStatus{}, ccsq.predicates...),
		// clone intermediate query.
		sql:  ccsq.sql.Clone(),
		path: ccsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CarID int `json:"car_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarConfigStatus.Query().
//		GroupBy(carconfigstatus.FieldCarID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ccsq *CarConfigStatusQuery) GroupBy(field string, fields ...string) *CarConfigStatusGroupBy {
	ccsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarConfigStatusGroupBy{build: ccsq}
	grbuild.flds = &ccsq.ctx.Fields
	grbuild.label = carconfigstatus.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CarID int `json:"car_id,omitempty"`
//	}
//
//	client.CarConfigStatus.Query().
//		Select(carconfigstatus.FieldCarID).
//		Scan(ctx, &v)
func (ccsq *CarConfigStatusQuery) Select(fields ...string) *CarConfigStatusSelect {
	ccsq.ctx.Fields = append(ccsq.ctx.Fields, fields...)
	sbuild := &CarConfigStatusSelect{CarConfigStatusQuery: ccsq}
	sbuild.label = carconfigstatus.Label
	sbuild.flds, sbuild.scan = &ccsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarConfigStatusSelect configured with the given aggregations.
func (ccsq *CarConfigStatusQuery) Aggregate(fns ...AggregateFunc) *CarConfigStatusSelect {
	return ccsq.Select().Aggregate(fns...)
}

func (ccsq *CarConfigStatusQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ccsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ccsq); err != nil {
				return err
			}
		}
	}
	for _, f := range ccsq.ctx.Fields {
		if !carconfigstatus.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ccsq.path != nil {
		prev, err := ccsq.path(ctx)
		if err != nil {
			return err
		}
		ccsq.sql = prev
	}
	return nil
}

func (ccsq *CarConfigStatusQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarConfigStatus, error) {
	var (
		nodes = []*CarConfigStatus{}
		_spec = ccsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarConfigStatus).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarConfigStatus{config: ccsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ccsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ccsq *CarConfigStatusQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ccsq.querySpec()
	_spec.Node.Columns = ccsq.ctx.Fields
	if len(ccsq.ctx.Fields) > 0 {
		_spec.Unique = ccsq.ctx.Unique != nil && *ccsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ccsq.driver, _spec)
}

func (ccsq *CarConfigStatusQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carconfigstatus.Table, carconfigstatus.Columns, sqlgraph.NewFieldSpec(carconfigstatus.FieldID, field.TypeInt))
	_spec.From = ccsq.sql
	if unique := ccsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ccsq.path != nil {
		_spec.Unique = true
	}
	if fields := ccsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carconfigstatus.FieldID)
		for i := range fields {
			if fields[i] != carconfigstatus.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ccsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ccsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ccsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ccsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ccsq *CarConfigStatusQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ccsq.driver.Dialect())
	t1 := builder.Table(carconfigstatus.Table)
	columns := ccsq.ctx.Fields
	if len(columns) == 0 {
		columns = carconfigstatus.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ccsq.sql != nil {
		selector = ccsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ccsq.ctx.Unique != nil && *ccsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ccsq.predicates {
		p(selector)
	}
	for _, p := range ccsq.order {
		p(selector)
	}
	if offset := ccsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ccsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CarConfigStatusGroupBy is the group-by builder for CarConfigStatus entities.
type CarConfigStatusGroupBy struct {
	selector
	build *CarConfigStatusQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ccsgb *CarConfigStatusGroupBy) Aggregate(fns ...AggregateFunc) *CarConfigStatusGroupBy {
	ccsgb.fns = append(ccsgb.fns, fns...)
	return ccsgb
}

// Scan applies the selector query and scans the result into the given value.
func (ccsgb *CarConfigStatusGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccsgb.build.ctx, ent.OpQueryGroupBy)
	if err := ccsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarConfigStatusQuery, *CarConfigStatusGroupBy](ctx, ccsgb.build, ccsgb, ccsgb.build.inters, v)
}

func (ccsgb *CarConfigStatusGroupBy) sqlScan(ctx context.Context, root *CarConfigStatusQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ccsgb.fns))
	for _, fn := range ccsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ccsgb.flds)+len(ccsgb.fns))
		for _, f := range *ccsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ccsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarConfigStatusSelect is the builder for selecting fields of CarConfigStatus entities.
type CarConfigStatusSelect struct {
	*CarConfigStatusQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ccss *CarConfigStatusSelect) Aggregate(fns ...AggregateFunc) *CarConfigStatusSelect {
	ccss.fns = append(ccss.fns, fns...)
	return ccss
}

// Scan applies the selector query and scans the result into the given value.
func (ccss *CarConfigStatusSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccss.ctx, ent.OpQuerySelect)
	if err := ccss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarConfigStatusQuery, *CarConfigStatusSelect](ctx, ccss.CarConfigStatusQuery, ccss, ccss.inters, v)
}

func (ccss *CarConfigStatusSelect) sqlScan(ctx context.Context, root *CarConfigStatusQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ccss.fns))
	for _, fn := range ccss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ccss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
