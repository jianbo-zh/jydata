// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"github.com/jianbo-zh/jydata/database/ent/statshourlyscenicarea"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// StatsHourlyScenicAreaQuery is the builder for querying StatsHourlyScenicArea entities.
type StatsHourlyScenicAreaQuery struct {
	config
	ctx        *QueryContext
	order      []statshourlyscenicarea.OrderOption
	inters     []Interceptor
	predicates []predicate.StatsHourlyScenicArea
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StatsHourlyScenicAreaQuery builder.
func (shsaq *StatsHourlyScenicAreaQuery) Where(ps ...predicate.StatsHourlyScenicArea) *StatsHourlyScenicAreaQuery {
	shsaq.predicates = append(shsaq.predicates, ps...)
	return shsaq
}

// Limit the number of records to be returned by this query.
func (shsaq *StatsHourlyScenicAreaQuery) Limit(limit int) *StatsHourlyScenicAreaQuery {
	shsaq.ctx.Limit = &limit
	return shsaq
}

// Offset to start from.
func (shsaq *StatsHourlyScenicAreaQuery) Offset(offset int) *StatsHourlyScenicAreaQuery {
	shsaq.ctx.Offset = &offset
	return shsaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (shsaq *StatsHourlyScenicAreaQuery) Unique(unique bool) *StatsHourlyScenicAreaQuery {
	shsaq.ctx.Unique = &unique
	return shsaq
}

// Order specifies how the records should be ordered.
func (shsaq *StatsHourlyScenicAreaQuery) Order(o ...statshourlyscenicarea.OrderOption) *StatsHourlyScenicAreaQuery {
	shsaq.order = append(shsaq.order, o...)
	return shsaq
}

// First returns the first StatsHourlyScenicArea entity from the query.
// Returns a *NotFoundError when no StatsHourlyScenicArea was found.
func (shsaq *StatsHourlyScenicAreaQuery) First(ctx context.Context) (*StatsHourlyScenicArea, error) {
	nodes, err := shsaq.Limit(1).All(setContextOp(ctx, shsaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{statshourlyscenicarea.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (shsaq *StatsHourlyScenicAreaQuery) FirstX(ctx context.Context) *StatsHourlyScenicArea {
	node, err := shsaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StatsHourlyScenicArea ID from the query.
// Returns a *NotFoundError when no StatsHourlyScenicArea ID was found.
func (shsaq *StatsHourlyScenicAreaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = shsaq.Limit(1).IDs(setContextOp(ctx, shsaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{statshourlyscenicarea.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (shsaq *StatsHourlyScenicAreaQuery) FirstIDX(ctx context.Context) int {
	id, err := shsaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StatsHourlyScenicArea entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StatsHourlyScenicArea entity is found.
// Returns a *NotFoundError when no StatsHourlyScenicArea entities are found.
func (shsaq *StatsHourlyScenicAreaQuery) Only(ctx context.Context) (*StatsHourlyScenicArea, error) {
	nodes, err := shsaq.Limit(2).All(setContextOp(ctx, shsaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{statshourlyscenicarea.Label}
	default:
		return nil, &NotSingularError{statshourlyscenicarea.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (shsaq *StatsHourlyScenicAreaQuery) OnlyX(ctx context.Context) *StatsHourlyScenicArea {
	node, err := shsaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StatsHourlyScenicArea ID in the query.
// Returns a *NotSingularError when more than one StatsHourlyScenicArea ID is found.
// Returns a *NotFoundError when no entities are found.
func (shsaq *StatsHourlyScenicAreaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = shsaq.Limit(2).IDs(setContextOp(ctx, shsaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{statshourlyscenicarea.Label}
	default:
		err = &NotSingularError{statshourlyscenicarea.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (shsaq *StatsHourlyScenicAreaQuery) OnlyIDX(ctx context.Context) int {
	id, err := shsaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StatsHourlyScenicAreas.
func (shsaq *StatsHourlyScenicAreaQuery) All(ctx context.Context) ([]*StatsHourlyScenicArea, error) {
	ctx = setContextOp(ctx, shsaq.ctx, ent.OpQueryAll)
	if err := shsaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StatsHourlyScenicArea, *StatsHourlyScenicAreaQuery]()
	return withInterceptors[[]*StatsHourlyScenicArea](ctx, shsaq, qr, shsaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (shsaq *StatsHourlyScenicAreaQuery) AllX(ctx context.Context) []*StatsHourlyScenicArea {
	nodes, err := shsaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StatsHourlyScenicArea IDs.
func (shsaq *StatsHourlyScenicAreaQuery) IDs(ctx context.Context) (ids []int, err error) {
	if shsaq.ctx.Unique == nil && shsaq.path != nil {
		shsaq.Unique(true)
	}
	ctx = setContextOp(ctx, shsaq.ctx, ent.OpQueryIDs)
	if err = shsaq.Select(statshourlyscenicarea.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (shsaq *StatsHourlyScenicAreaQuery) IDsX(ctx context.Context) []int {
	ids, err := shsaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (shsaq *StatsHourlyScenicAreaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, shsaq.ctx, ent.OpQueryCount)
	if err := shsaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, shsaq, querierCount[*StatsHourlyScenicAreaQuery](), shsaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (shsaq *StatsHourlyScenicAreaQuery) CountX(ctx context.Context) int {
	count, err := shsaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (shsaq *StatsHourlyScenicAreaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, shsaq.ctx, ent.OpQueryExist)
	switch _, err := shsaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (shsaq *StatsHourlyScenicAreaQuery) ExistX(ctx context.Context) bool {
	exist, err := shsaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StatsHourlyScenicAreaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (shsaq *StatsHourlyScenicAreaQuery) Clone() *StatsHourlyScenicAreaQuery {
	if shsaq == nil {
		return nil
	}
	return &StatsHourlyScenicAreaQuery{
		config:     shsaq.config,
		ctx:        shsaq.ctx.Clone(),
		order:      append([]statshourlyscenicarea.OrderOption{}, shsaq.order...),
		inters:     append([]Interceptor{}, shsaq.inters...),
		predicates: append([]predicate.StatsHourlyScenicArea{}, shsaq.predicates...),
		// clone intermediate query.
		sql:  shsaq.sql.Clone(),
		path: shsaq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StatsHourlyScenicArea.Query().
//		GroupBy(statshourlyscenicarea.FieldScenicAreaID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (shsaq *StatsHourlyScenicAreaQuery) GroupBy(field string, fields ...string) *StatsHourlyScenicAreaGroupBy {
	shsaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StatsHourlyScenicAreaGroupBy{build: shsaq}
	grbuild.flds = &shsaq.ctx.Fields
	grbuild.label = statshourlyscenicarea.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//	}
//
//	client.StatsHourlyScenicArea.Query().
//		Select(statshourlyscenicarea.FieldScenicAreaID).
//		Scan(ctx, &v)
func (shsaq *StatsHourlyScenicAreaQuery) Select(fields ...string) *StatsHourlyScenicAreaSelect {
	shsaq.ctx.Fields = append(shsaq.ctx.Fields, fields...)
	sbuild := &StatsHourlyScenicAreaSelect{StatsHourlyScenicAreaQuery: shsaq}
	sbuild.label = statshourlyscenicarea.Label
	sbuild.flds, sbuild.scan = &shsaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StatsHourlyScenicAreaSelect configured with the given aggregations.
func (shsaq *StatsHourlyScenicAreaQuery) Aggregate(fns ...AggregateFunc) *StatsHourlyScenicAreaSelect {
	return shsaq.Select().Aggregate(fns...)
}

func (shsaq *StatsHourlyScenicAreaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range shsaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, shsaq); err != nil {
				return err
			}
		}
	}
	for _, f := range shsaq.ctx.Fields {
		if !statshourlyscenicarea.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if shsaq.path != nil {
		prev, err := shsaq.path(ctx)
		if err != nil {
			return err
		}
		shsaq.sql = prev
	}
	return nil
}

func (shsaq *StatsHourlyScenicAreaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StatsHourlyScenicArea, error) {
	var (
		nodes = []*StatsHourlyScenicArea{}
		_spec = shsaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StatsHourlyScenicArea).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StatsHourlyScenicArea{config: shsaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, shsaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (shsaq *StatsHourlyScenicAreaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := shsaq.querySpec()
	_spec.Node.Columns = shsaq.ctx.Fields
	if len(shsaq.ctx.Fields) > 0 {
		_spec.Unique = shsaq.ctx.Unique != nil && *shsaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, shsaq.driver, _spec)
}

func (shsaq *StatsHourlyScenicAreaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(statshourlyscenicarea.Table, statshourlyscenicarea.Columns, sqlgraph.NewFieldSpec(statshourlyscenicarea.FieldID, field.TypeInt))
	_spec.From = shsaq.sql
	if unique := shsaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if shsaq.path != nil {
		_spec.Unique = true
	}
	if fields := shsaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, statshourlyscenicarea.FieldID)
		for i := range fields {
			if fields[i] != statshourlyscenicarea.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := shsaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := shsaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := shsaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := shsaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (shsaq *StatsHourlyScenicAreaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(shsaq.driver.Dialect())
	t1 := builder.Table(statshourlyscenicarea.Table)
	columns := shsaq.ctx.Fields
	if len(columns) == 0 {
		columns = statshourlyscenicarea.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if shsaq.sql != nil {
		selector = shsaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if shsaq.ctx.Unique != nil && *shsaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range shsaq.predicates {
		p(selector)
	}
	for _, p := range shsaq.order {
		p(selector)
	}
	if offset := shsaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := shsaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StatsHourlyScenicAreaGroupBy is the group-by builder for StatsHourlyScenicArea entities.
type StatsHourlyScenicAreaGroupBy struct {
	selector
	build *StatsHourlyScenicAreaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (shsagb *StatsHourlyScenicAreaGroupBy) Aggregate(fns ...AggregateFunc) *StatsHourlyScenicAreaGroupBy {
	shsagb.fns = append(shsagb.fns, fns...)
	return shsagb
}

// Scan applies the selector query and scans the result into the given value.
func (shsagb *StatsHourlyScenicAreaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, shsagb.build.ctx, ent.OpQueryGroupBy)
	if err := shsagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsHourlyScenicAreaQuery, *StatsHourlyScenicAreaGroupBy](ctx, shsagb.build, shsagb, shsagb.build.inters, v)
}

func (shsagb *StatsHourlyScenicAreaGroupBy) sqlScan(ctx context.Context, root *StatsHourlyScenicAreaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(shsagb.fns))
	for _, fn := range shsagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*shsagb.flds)+len(shsagb.fns))
		for _, f := range *shsagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*shsagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := shsagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StatsHourlyScenicAreaSelect is the builder for selecting fields of StatsHourlyScenicArea entities.
type StatsHourlyScenicAreaSelect struct {
	*StatsHourlyScenicAreaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (shsas *StatsHourlyScenicAreaSelect) Aggregate(fns ...AggregateFunc) *StatsHourlyScenicAreaSelect {
	shsas.fns = append(shsas.fns, fns...)
	return shsas
}

// Scan applies the selector query and scans the result into the given value.
func (shsas *StatsHourlyScenicAreaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, shsas.ctx, ent.OpQuerySelect)
	if err := shsas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsHourlyScenicAreaQuery, *StatsHourlyScenicAreaSelect](ctx, shsas.StatsHourlyScenicAreaQuery, shsas, shsas.inters, v)
}

func (shsas *StatsHourlyScenicAreaSelect) sqlScan(ctx context.Context, root *StatsHourlyScenicAreaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(shsas.fns))
	for _, fn := range shsas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*shsas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := shsas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
