// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/jianbo-zh/jydata/database/ent/carsflight"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CarsFlightQuery is the builder for querying CarsFlight entities.
type CarsFlightQuery struct {
	config
	ctx        *QueryContext
	order      []carsflight.OrderOption
	inters     []Interceptor
	predicates []predicate.CarsFlight
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarsFlightQuery builder.
func (cfq *CarsFlightQuery) Where(ps ...predicate.CarsFlight) *CarsFlightQuery {
	cfq.predicates = append(cfq.predicates, ps...)
	return cfq
}

// Limit the number of records to be returned by this query.
func (cfq *CarsFlightQuery) Limit(limit int) *CarsFlightQuery {
	cfq.ctx.Limit = &limit
	return cfq
}

// Offset to start from.
func (cfq *CarsFlightQuery) Offset(offset int) *CarsFlightQuery {
	cfq.ctx.Offset = &offset
	return cfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cfq *CarsFlightQuery) Unique(unique bool) *CarsFlightQuery {
	cfq.ctx.Unique = &unique
	return cfq
}

// Order specifies how the records should be ordered.
func (cfq *CarsFlightQuery) Order(o ...carsflight.OrderOption) *CarsFlightQuery {
	cfq.order = append(cfq.order, o...)
	return cfq
}

// First returns the first CarsFlight entity from the query.
// Returns a *NotFoundError when no CarsFlight was found.
func (cfq *CarsFlightQuery) First(ctx context.Context) (*CarsFlight, error) {
	nodes, err := cfq.Limit(1).All(setContextOp(ctx, cfq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carsflight.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cfq *CarsFlightQuery) FirstX(ctx context.Context) *CarsFlight {
	node, err := cfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarsFlight ID from the query.
// Returns a *NotFoundError when no CarsFlight ID was found.
func (cfq *CarsFlightQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfq.Limit(1).IDs(setContextOp(ctx, cfq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carsflight.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cfq *CarsFlightQuery) FirstIDX(ctx context.Context) int {
	id, err := cfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarsFlight entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarsFlight entity is found.
// Returns a *NotFoundError when no CarsFlight entities are found.
func (cfq *CarsFlightQuery) Only(ctx context.Context) (*CarsFlight, error) {
	nodes, err := cfq.Limit(2).All(setContextOp(ctx, cfq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carsflight.Label}
	default:
		return nil, &NotSingularError{carsflight.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cfq *CarsFlightQuery) OnlyX(ctx context.Context) *CarsFlight {
	node, err := cfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarsFlight ID in the query.
// Returns a *NotSingularError when more than one CarsFlight ID is found.
// Returns a *NotFoundError when no entities are found.
func (cfq *CarsFlightQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfq.Limit(2).IDs(setContextOp(ctx, cfq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carsflight.Label}
	default:
		err = &NotSingularError{carsflight.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cfq *CarsFlightQuery) OnlyIDX(ctx context.Context) int {
	id, err := cfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarsFlights.
func (cfq *CarsFlightQuery) All(ctx context.Context) ([]*CarsFlight, error) {
	ctx = setContextOp(ctx, cfq.ctx, ent.OpQueryAll)
	if err := cfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarsFlight, *CarsFlightQuery]()
	return withInterceptors[[]*CarsFlight](ctx, cfq, qr, cfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cfq *CarsFlightQuery) AllX(ctx context.Context) []*CarsFlight {
	nodes, err := cfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarsFlight IDs.
func (cfq *CarsFlightQuery) IDs(ctx context.Context) (ids []int, err error) {
	if cfq.ctx.Unique == nil && cfq.path != nil {
		cfq.Unique(true)
	}
	ctx = setContextOp(ctx, cfq.ctx, ent.OpQueryIDs)
	if err = cfq.Select(carsflight.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cfq *CarsFlightQuery) IDsX(ctx context.Context) []int {
	ids, err := cfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cfq *CarsFlightQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cfq.ctx, ent.OpQueryCount)
	if err := cfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cfq, querierCount[*CarsFlightQuery](), cfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cfq *CarsFlightQuery) CountX(ctx context.Context) int {
	count, err := cfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cfq *CarsFlightQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cfq.ctx, ent.OpQueryExist)
	switch _, err := cfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cfq *CarsFlightQuery) ExistX(ctx context.Context) bool {
	exist, err := cfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarsFlightQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cfq *CarsFlightQuery) Clone() *CarsFlightQuery {
	if cfq == nil {
		return nil
	}
	return &CarsFlightQuery{
		config:     cfq.config,
		ctx:        cfq.ctx.Clone(),
		order:      append([]carsflight.OrderOption{}, cfq.order...),
		inters:     append([]Interceptor{}, cfq.inters...),
		predicates: append([]predicate.CarsFlight{}, cfq.predicates...),
		// clone intermediate query.
		sql:  cfq.sql.Clone(),
		path: cfq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarsFlight.Query().
//		GroupBy(carsflight.FieldScenicAreaID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cfq *CarsFlightQuery) GroupBy(field string, fields ...string) *CarsFlightGroupBy {
	cfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarsFlightGroupBy{build: cfq}
	grbuild.flds = &cfq.ctx.Fields
	grbuild.label = carsflight.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//	}
//
//	client.CarsFlight.Query().
//		Select(carsflight.FieldScenicAreaID).
//		Scan(ctx, &v)
func (cfq *CarsFlightQuery) Select(fields ...string) *CarsFlightSelect {
	cfq.ctx.Fields = append(cfq.ctx.Fields, fields...)
	sbuild := &CarsFlightSelect{CarsFlightQuery: cfq}
	sbuild.label = carsflight.Label
	sbuild.flds, sbuild.scan = &cfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarsFlightSelect configured with the given aggregations.
func (cfq *CarsFlightQuery) Aggregate(fns ...AggregateFunc) *CarsFlightSelect {
	return cfq.Select().Aggregate(fns...)
}

func (cfq *CarsFlightQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cfq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cfq); err != nil {
				return err
			}
		}
	}
	for _, f := range cfq.ctx.Fields {
		if !carsflight.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cfq.path != nil {
		prev, err := cfq.path(ctx)
		if err != nil {
			return err
		}
		cfq.sql = prev
	}
	return nil
}

func (cfq *CarsFlightQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarsFlight, error) {
	var (
		nodes = []*CarsFlight{}
		_spec = cfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarsFlight).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarsFlight{config: cfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cfq *CarsFlightQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cfq.querySpec()
	_spec.Node.Columns = cfq.ctx.Fields
	if len(cfq.ctx.Fields) > 0 {
		_spec.Unique = cfq.ctx.Unique != nil && *cfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cfq.driver, _spec)
}

func (cfq *CarsFlightQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carsflight.Table, carsflight.Columns, sqlgraph.NewFieldSpec(carsflight.FieldID, field.TypeInt))
	_spec.From = cfq.sql
	if unique := cfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cfq.path != nil {
		_spec.Unique = true
	}
	if fields := cfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carsflight.FieldID)
		for i := range fields {
			if fields[i] != carsflight.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cfq *CarsFlightQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cfq.driver.Dialect())
	t1 := builder.Table(carsflight.Table)
	columns := cfq.ctx.Fields
	if len(columns) == 0 {
		columns = carsflight.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cfq.sql != nil {
		selector = cfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cfq.ctx.Unique != nil && *cfq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cfq.predicates {
		p(selector)
	}
	for _, p := range cfq.order {
		p(selector)
	}
	if offset := cfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CarsFlightGroupBy is the group-by builder for CarsFlight entities.
type CarsFlightGroupBy struct {
	selector
	build *CarsFlightQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cfgb *CarsFlightGroupBy) Aggregate(fns ...AggregateFunc) *CarsFlightGroupBy {
	cfgb.fns = append(cfgb.fns, fns...)
	return cfgb
}

// Scan applies the selector query and scans the result into the given value.
func (cfgb *CarsFlightGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cfgb.build.ctx, ent.OpQueryGroupBy)
	if err := cfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarsFlightQuery, *CarsFlightGroupBy](ctx, cfgb.build, cfgb, cfgb.build.inters, v)
}

func (cfgb *CarsFlightGroupBy) sqlScan(ctx context.Context, root *CarsFlightQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cfgb.fns))
	for _, fn := range cfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cfgb.flds)+len(cfgb.fns))
		for _, f := range *cfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarsFlightSelect is the builder for selecting fields of CarsFlight entities.
type CarsFlightSelect struct {
	*CarsFlightQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cfs *CarsFlightSelect) Aggregate(fns ...AggregateFunc) *CarsFlightSelect {
	cfs.fns = append(cfs.fns, fns...)
	return cfs
}

// Scan applies the selector query and scans the result into the given value.
func (cfs *CarsFlightSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cfs.ctx, ent.OpQuerySelect)
	if err := cfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarsFlightQuery, *CarsFlightSelect](ctx, cfs.CarsFlightQuery, cfs, cfs.inters, v)
}

func (cfs *CarsFlightSelect) sqlScan(ctx context.Context, root *CarsFlightQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cfs.fns))
	for _, fn := range cfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
