// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jianbo-zh/jydata/database/ent/car"
	"github.com/jianbo-zh/jydata/database/ent/carconfig"
	"github.com/jianbo-zh/jydata/database/ent/carsmodels"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"github.com/jianbo-zh/jydata/database/ent/scenicarea"
)

// CarConfigQuery is the builder for querying CarConfig entities.
type CarConfigQuery struct {
	config
	ctx                      *QueryContext
	order                    []carconfig.OrderOption
	inters                   []Interceptor
	predicates               []predicate.CarConfig
	withBackgroundScenicArea *ScenicAreaQuery
	withCarsModels           *CarsModelsQuery
	withCar                  *CarQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarConfigQuery builder.
func (ccq *CarConfigQuery) Where(ps ...predicate.CarConfig) *CarConfigQuery {
	ccq.predicates = append(ccq.predicates, ps...)
	return ccq
}

// Limit the number of records to be returned by this query.
func (ccq *CarConfigQuery) Limit(limit int) *CarConfigQuery {
	ccq.ctx.Limit = &limit
	return ccq
}

// Offset to start from.
func (ccq *CarConfigQuery) Offset(offset int) *CarConfigQuery {
	ccq.ctx.Offset = &offset
	return ccq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ccq *CarConfigQuery) Unique(unique bool) *CarConfigQuery {
	ccq.ctx.Unique = &unique
	return ccq
}

// Order specifies how the records should be ordered.
func (ccq *CarConfigQuery) Order(o ...carconfig.OrderOption) *CarConfigQuery {
	ccq.order = append(ccq.order, o...)
	return ccq
}

// QueryBackgroundScenicArea chains the current query on the "background_scenic_area" edge.
func (ccq *CarConfigQuery) QueryBackgroundScenicArea() *ScenicAreaQuery {
	query := (&ScenicAreaClient{config: ccq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ccq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ccq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carconfig.Table, carconfig.FieldID, selector),
			sqlgraph.To(scenicarea.Table, scenicarea.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, carconfig.BackgroundScenicAreaTable, carconfig.BackgroundScenicAreaColumn),
		)
		fromU = sqlgraph.SetNeighbors(ccq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCarsModels chains the current query on the "cars_models" edge.
func (ccq *CarConfigQuery) QueryCarsModels() *CarsModelsQuery {
	query := (&CarsModelsClient{config: ccq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ccq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ccq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carconfig.Table, carconfig.FieldID, selector),
			sqlgraph.To(carsmodels.Table, carsmodels.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, carconfig.CarsModelsTable, carconfig.CarsModelsColumn),
		)
		fromU = sqlgraph.SetNeighbors(ccq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCar chains the current query on the "car" edge.
func (ccq *CarConfigQuery) QueryCar() *CarQuery {
	query := (&CarClient{config: ccq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ccq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ccq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(carconfig.Table, carconfig.FieldID, selector),
			sqlgraph.To(car.Table, car.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, carconfig.CarTable, carconfig.CarColumn),
		)
		fromU = sqlgraph.SetNeighbors(ccq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CarConfig entity from the query.
// Returns a *NotFoundError when no CarConfig was found.
func (ccq *CarConfigQuery) First(ctx context.Context) (*CarConfig, error) {
	nodes, err := ccq.Limit(1).All(setContextOp(ctx, ccq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ccq *CarConfigQuery) FirstX(ctx context.Context) *CarConfig {
	node, err := ccq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarConfig ID from the query.
// Returns a *NotFoundError when no CarConfig ID was found.
func (ccq *CarConfigQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ccq.Limit(1).IDs(setContextOp(ctx, ccq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ccq *CarConfigQuery) FirstIDX(ctx context.Context) int {
	id, err := ccq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarConfig entity is found.
// Returns a *NotFoundError when no CarConfig entities are found.
func (ccq *CarConfigQuery) Only(ctx context.Context) (*CarConfig, error) {
	nodes, err := ccq.Limit(2).All(setContextOp(ctx, ccq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carconfig.Label}
	default:
		return nil, &NotSingularError{carconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ccq *CarConfigQuery) OnlyX(ctx context.Context) *CarConfig {
	node, err := ccq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarConfig ID in the query.
// Returns a *NotSingularError when more than one CarConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (ccq *CarConfigQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ccq.Limit(2).IDs(setContextOp(ctx, ccq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carconfig.Label}
	default:
		err = &NotSingularError{carconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ccq *CarConfigQuery) OnlyIDX(ctx context.Context) int {
	id, err := ccq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarConfigs.
func (ccq *CarConfigQuery) All(ctx context.Context) ([]*CarConfig, error) {
	ctx = setContextOp(ctx, ccq.ctx, ent.OpQueryAll)
	if err := ccq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarConfig, *CarConfigQuery]()
	return withInterceptors[[]*CarConfig](ctx, ccq, qr, ccq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ccq *CarConfigQuery) AllX(ctx context.Context) []*CarConfig {
	nodes, err := ccq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarConfig IDs.
func (ccq *CarConfigQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ccq.ctx.Unique == nil && ccq.path != nil {
		ccq.Unique(true)
	}
	ctx = setContextOp(ctx, ccq.ctx, ent.OpQueryIDs)
	if err = ccq.Select(carconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ccq *CarConfigQuery) IDsX(ctx context.Context) []int {
	ids, err := ccq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ccq *CarConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ccq.ctx, ent.OpQueryCount)
	if err := ccq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ccq, querierCount[*CarConfigQuery](), ccq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ccq *CarConfigQuery) CountX(ctx context.Context) int {
	count, err := ccq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ccq *CarConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ccq.ctx, ent.OpQueryExist)
	switch _, err := ccq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ccq *CarConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := ccq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ccq *CarConfigQuery) Clone() *CarConfigQuery {
	if ccq == nil {
		return nil
	}
	return &CarConfigQuery{
		config:                   ccq.config,
		ctx:                      ccq.ctx.Clone(),
		order:                    append([]carconfig.OrderOption{}, ccq.order...),
		inters:                   append([]Interceptor{}, ccq.inters...),
		predicates:               append([]predicate.CarConfig{}, ccq.predicates...),
		withBackgroundScenicArea: ccq.withBackgroundScenicArea.Clone(),
		withCarsModels:           ccq.withCarsModels.Clone(),
		withCar:                  ccq.withCar.Clone(),
		// clone intermediate query.
		sql:  ccq.sql.Clone(),
		path: ccq.path,
	}
}

// WithBackgroundScenicArea tells the query-builder to eager-load the nodes that are connected to
// the "background_scenic_area" edge. The optional arguments are used to configure the query builder of the edge.
func (ccq *CarConfigQuery) WithBackgroundScenicArea(opts ...func(*ScenicAreaQuery)) *CarConfigQuery {
	query := (&ScenicAreaClient{config: ccq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ccq.withBackgroundScenicArea = query
	return ccq
}

// WithCarsModels tells the query-builder to eager-load the nodes that are connected to
// the "cars_models" edge. The optional arguments are used to configure the query builder of the edge.
func (ccq *CarConfigQuery) WithCarsModels(opts ...func(*CarsModelsQuery)) *CarConfigQuery {
	query := (&CarsModelsClient{config: ccq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ccq.withCarsModels = query
	return ccq
}

// WithCar tells the query-builder to eager-load the nodes that are connected to
// the "car" edge. The optional arguments are used to configure the query builder of the edge.
func (ccq *CarConfigQuery) WithCar(opts ...func(*CarQuery)) *CarConfigQuery {
	query := (&CarClient{config: ccq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ccq.withCar = query
	return ccq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarConfig.Query().
//		GroupBy(carconfig.FieldScenicAreaID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ccq *CarConfigQuery) GroupBy(field string, fields ...string) *CarConfigGroupBy {
	ccq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarConfigGroupBy{build: ccq}
	grbuild.flds = &ccq.ctx.Fields
	grbuild.label = carconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//	}
//
//	client.CarConfig.Query().
//		Select(carconfig.FieldScenicAreaID).
//		Scan(ctx, &v)
func (ccq *CarConfigQuery) Select(fields ...string) *CarConfigSelect {
	ccq.ctx.Fields = append(ccq.ctx.Fields, fields...)
	sbuild := &CarConfigSelect{CarConfigQuery: ccq}
	sbuild.label = carconfig.Label
	sbuild.flds, sbuild.scan = &ccq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarConfigSelect configured with the given aggregations.
func (ccq *CarConfigQuery) Aggregate(fns ...AggregateFunc) *CarConfigSelect {
	return ccq.Select().Aggregate(fns...)
}

func (ccq *CarConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ccq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ccq); err != nil {
				return err
			}
		}
	}
	for _, f := range ccq.ctx.Fields {
		if !carconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ccq.path != nil {
		prev, err := ccq.path(ctx)
		if err != nil {
			return err
		}
		ccq.sql = prev
	}
	return nil
}

func (ccq *CarConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarConfig, error) {
	var (
		nodes       = []*CarConfig{}
		_spec       = ccq.querySpec()
		loadedTypes = [3]bool{
			ccq.withBackgroundScenicArea != nil,
			ccq.withCarsModels != nil,
			ccq.withCar != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarConfig{config: ccq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ccq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ccq.withBackgroundScenicArea; query != nil {
		if err := ccq.loadBackgroundScenicArea(ctx, query, nodes, nil,
			func(n *CarConfig, e *ScenicArea) { n.Edges.BackgroundScenicArea = e }); err != nil {
			return nil, err
		}
	}
	if query := ccq.withCarsModels; query != nil {
		if err := ccq.loadCarsModels(ctx, query, nodes, nil,
			func(n *CarConfig, e *CarsModels) { n.Edges.CarsModels = e }); err != nil {
			return nil, err
		}
	}
	if query := ccq.withCar; query != nil {
		if err := ccq.loadCar(ctx, query, nodes, nil,
			func(n *CarConfig, e *Car) { n.Edges.Car = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ccq *CarConfigQuery) loadBackgroundScenicArea(ctx context.Context, query *ScenicAreaQuery, nodes []*CarConfig, init func(*CarConfig), assign func(*CarConfig, *ScenicArea)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CarConfig)
	for i := range nodes {
		if nodes[i].ScenicAreaID == nil {
			continue
		}
		fk := *nodes[i].ScenicAreaID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(scenicarea.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "scenic_area_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ccq *CarConfigQuery) loadCarsModels(ctx context.Context, query *CarsModelsQuery, nodes []*CarConfig, init func(*CarConfig), assign func(*CarConfig, *CarsModels)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CarConfig)
	for i := range nodes {
		if nodes[i].ModelID == nil {
			continue
		}
		fk := *nodes[i].ModelID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(carsmodels.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "model_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ccq *CarConfigQuery) loadCar(ctx context.Context, query *CarQuery, nodes []*CarConfig, init func(*CarConfig), assign func(*CarConfig, *Car)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CarConfig)
	for i := range nodes {
		if nodes[i].CarID == nil {
			continue
		}
		fk := *nodes[i].CarID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(car.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "car_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ccq *CarConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ccq.querySpec()
	_spec.Node.Columns = ccq.ctx.Fields
	if len(ccq.ctx.Fields) > 0 {
		_spec.Unique = ccq.ctx.Unique != nil && *ccq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ccq.driver, _spec)
}

func (ccq *CarConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carconfig.Table, carconfig.Columns, sqlgraph.NewFieldSpec(carconfig.FieldID, field.TypeInt))
	_spec.From = ccq.sql
	if unique := ccq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ccq.path != nil {
		_spec.Unique = true
	}
	if fields := ccq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carconfig.FieldID)
		for i := range fields {
			if fields[i] != carconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ccq.withBackgroundScenicArea != nil {
			_spec.Node.AddColumnOnce(carconfig.FieldScenicAreaID)
		}
		if ccq.withCarsModels != nil {
			_spec.Node.AddColumnOnce(carconfig.FieldModelID)
		}
		if ccq.withCar != nil {
			_spec.Node.AddColumnOnce(carconfig.FieldCarID)
		}
	}
	if ps := ccq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ccq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ccq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ccq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ccq *CarConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ccq.driver.Dialect())
	t1 := builder.Table(carconfig.Table)
	columns := ccq.ctx.Fields
	if len(columns) == 0 {
		columns = carconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ccq.sql != nil {
		selector = ccq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ccq.ctx.Unique != nil && *ccq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ccq.predicates {
		p(selector)
	}
	for _, p := range ccq.order {
		p(selector)
	}
	if offset := ccq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ccq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CarConfigGroupBy is the group-by builder for CarConfig entities.
type CarConfigGroupBy struct {
	selector
	build *CarConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ccgb *CarConfigGroupBy) Aggregate(fns ...AggregateFunc) *CarConfigGroupBy {
	ccgb.fns = append(ccgb.fns, fns...)
	return ccgb
}

// Scan applies the selector query and scans the result into the given value.
func (ccgb *CarConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccgb.build.ctx, ent.OpQueryGroupBy)
	if err := ccgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarConfigQuery, *CarConfigGroupBy](ctx, ccgb.build, ccgb, ccgb.build.inters, v)
}

func (ccgb *CarConfigGroupBy) sqlScan(ctx context.Context, root *CarConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ccgb.fns))
	for _, fn := range ccgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ccgb.flds)+len(ccgb.fns))
		for _, f := range *ccgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ccgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarConfigSelect is the builder for selecting fields of CarConfig entities.
type CarConfigSelect struct {
	*CarConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ccs *CarConfigSelect) Aggregate(fns ...AggregateFunc) *CarConfigSelect {
	ccs.fns = append(ccs.fns, fns...)
	return ccs
}

// Scan applies the selector query and scans the result into the given value.
func (ccs *CarConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ccs.ctx, ent.OpQuerySelect)
	if err := ccs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarConfigQuery, *CarConfigSelect](ctx, ccs.CarConfigQuery, ccs, ccs.inters, v)
}

func (ccs *CarConfigSelect) sqlScan(ctx context.Context, root *CarConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ccs.fns))
	for _, fn := range ccs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ccs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ccs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
