// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jianbo-zh/jydata/database/ent/otaversion"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
)

// OtaVersionQuery is the builder for querying OtaVersion entities.
type OtaVersionQuery struct {
	config
	ctx        *QueryContext
	order      []otaversion.OrderOption
	inters     []Interceptor
	predicates []predicate.OtaVersion
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OtaVersionQuery builder.
func (ovq *OtaVersionQuery) Where(ps ...predicate.OtaVersion) *OtaVersionQuery {
	ovq.predicates = append(ovq.predicates, ps...)
	return ovq
}

// Limit the number of records to be returned by this query.
func (ovq *OtaVersionQuery) Limit(limit int) *OtaVersionQuery {
	ovq.ctx.Limit = &limit
	return ovq
}

// Offset to start from.
func (ovq *OtaVersionQuery) Offset(offset int) *OtaVersionQuery {
	ovq.ctx.Offset = &offset
	return ovq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ovq *OtaVersionQuery) Unique(unique bool) *OtaVersionQuery {
	ovq.ctx.Unique = &unique
	return ovq
}

// Order specifies how the records should be ordered.
func (ovq *OtaVersionQuery) Order(o ...otaversion.OrderOption) *OtaVersionQuery {
	ovq.order = append(ovq.order, o...)
	return ovq
}

// First returns the first OtaVersion entity from the query.
// Returns a *NotFoundError when no OtaVersion was found.
func (ovq *OtaVersionQuery) First(ctx context.Context) (*OtaVersion, error) {
	nodes, err := ovq.Limit(1).All(setContextOp(ctx, ovq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{otaversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ovq *OtaVersionQuery) FirstX(ctx context.Context) *OtaVersion {
	node, err := ovq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OtaVersion ID from the query.
// Returns a *NotFoundError when no OtaVersion ID was found.
func (ovq *OtaVersionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ovq.Limit(1).IDs(setContextOp(ctx, ovq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{otaversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ovq *OtaVersionQuery) FirstIDX(ctx context.Context) int {
	id, err := ovq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OtaVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OtaVersion entity is found.
// Returns a *NotFoundError when no OtaVersion entities are found.
func (ovq *OtaVersionQuery) Only(ctx context.Context) (*OtaVersion, error) {
	nodes, err := ovq.Limit(2).All(setContextOp(ctx, ovq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{otaversion.Label}
	default:
		return nil, &NotSingularError{otaversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ovq *OtaVersionQuery) OnlyX(ctx context.Context) *OtaVersion {
	node, err := ovq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OtaVersion ID in the query.
// Returns a *NotSingularError when more than one OtaVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (ovq *OtaVersionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ovq.Limit(2).IDs(setContextOp(ctx, ovq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{otaversion.Label}
	default:
		err = &NotSingularError{otaversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ovq *OtaVersionQuery) OnlyIDX(ctx context.Context) int {
	id, err := ovq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OtaVersions.
func (ovq *OtaVersionQuery) All(ctx context.Context) ([]*OtaVersion, error) {
	ctx = setContextOp(ctx, ovq.ctx, ent.OpQueryAll)
	if err := ovq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OtaVersion, *OtaVersionQuery]()
	return withInterceptors[[]*OtaVersion](ctx, ovq, qr, ovq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ovq *OtaVersionQuery) AllX(ctx context.Context) []*OtaVersion {
	nodes, err := ovq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OtaVersion IDs.
func (ovq *OtaVersionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ovq.ctx.Unique == nil && ovq.path != nil {
		ovq.Unique(true)
	}
	ctx = setContextOp(ctx, ovq.ctx, ent.OpQueryIDs)
	if err = ovq.Select(otaversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ovq *OtaVersionQuery) IDsX(ctx context.Context) []int {
	ids, err := ovq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ovq *OtaVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ovq.ctx, ent.OpQueryCount)
	if err := ovq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ovq, querierCount[*OtaVersionQuery](), ovq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ovq *OtaVersionQuery) CountX(ctx context.Context) int {
	count, err := ovq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ovq *OtaVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ovq.ctx, ent.OpQueryExist)
	switch _, err := ovq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ovq *OtaVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := ovq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OtaVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ovq *OtaVersionQuery) Clone() *OtaVersionQuery {
	if ovq == nil {
		return nil
	}
	return &OtaVersionQuery{
		config:     ovq.config,
		ctx:        ovq.ctx.Clone(),
		order:      append([]otaversion.OrderOption{}, ovq.order...),
		inters:     append([]Interceptor{}, ovq.inters...),
		predicates: append([]predicate.OtaVersion{}, ovq.predicates...),
		// clone intermediate query.
		sql:  ovq.sql.Clone(),
		path: ovq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeleteTime time.Time `json:"delete_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OtaVersion.Query().
//		GroupBy(otaversion.FieldDeleteTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ovq *OtaVersionQuery) GroupBy(field string, fields ...string) *OtaVersionGroupBy {
	ovq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OtaVersionGroupBy{build: ovq}
	grbuild.flds = &ovq.ctx.Fields
	grbuild.label = otaversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeleteTime time.Time `json:"delete_time,omitempty"`
//	}
//
//	client.OtaVersion.Query().
//		Select(otaversion.FieldDeleteTime).
//		Scan(ctx, &v)
func (ovq *OtaVersionQuery) Select(fields ...string) *OtaVersionSelect {
	ovq.ctx.Fields = append(ovq.ctx.Fields, fields...)
	sbuild := &OtaVersionSelect{OtaVersionQuery: ovq}
	sbuild.label = otaversion.Label
	sbuild.flds, sbuild.scan = &ovq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OtaVersionSelect configured with the given aggregations.
func (ovq *OtaVersionQuery) Aggregate(fns ...AggregateFunc) *OtaVersionSelect {
	return ovq.Select().Aggregate(fns...)
}

func (ovq *OtaVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ovq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ovq); err != nil {
				return err
			}
		}
	}
	for _, f := range ovq.ctx.Fields {
		if !otaversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ovq.path != nil {
		prev, err := ovq.path(ctx)
		if err != nil {
			return err
		}
		ovq.sql = prev
	}
	return nil
}

func (ovq *OtaVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OtaVersion, error) {
	var (
		nodes = []*OtaVersion{}
		_spec = ovq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OtaVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OtaVersion{config: ovq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ovq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ovq *OtaVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ovq.querySpec()
	_spec.Node.Columns = ovq.ctx.Fields
	if len(ovq.ctx.Fields) > 0 {
		_spec.Unique = ovq.ctx.Unique != nil && *ovq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ovq.driver, _spec)
}

func (ovq *OtaVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(otaversion.Table, otaversion.Columns, sqlgraph.NewFieldSpec(otaversion.FieldID, field.TypeInt))
	_spec.From = ovq.sql
	if unique := ovq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ovq.path != nil {
		_spec.Unique = true
	}
	if fields := ovq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, otaversion.FieldID)
		for i := range fields {
			if fields[i] != otaversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ovq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ovq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ovq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ovq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ovq *OtaVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ovq.driver.Dialect())
	t1 := builder.Table(otaversion.Table)
	columns := ovq.ctx.Fields
	if len(columns) == 0 {
		columns = otaversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ovq.sql != nil {
		selector = ovq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ovq.ctx.Unique != nil && *ovq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ovq.predicates {
		p(selector)
	}
	for _, p := range ovq.order {
		p(selector)
	}
	if offset := ovq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ovq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OtaVersionGroupBy is the group-by builder for OtaVersion entities.
type OtaVersionGroupBy struct {
	selector
	build *OtaVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ovgb *OtaVersionGroupBy) Aggregate(fns ...AggregateFunc) *OtaVersionGroupBy {
	ovgb.fns = append(ovgb.fns, fns...)
	return ovgb
}

// Scan applies the selector query and scans the result into the given value.
func (ovgb *OtaVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ovgb.build.ctx, ent.OpQueryGroupBy)
	if err := ovgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OtaVersionQuery, *OtaVersionGroupBy](ctx, ovgb.build, ovgb, ovgb.build.inters, v)
}

func (ovgb *OtaVersionGroupBy) sqlScan(ctx context.Context, root *OtaVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ovgb.fns))
	for _, fn := range ovgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ovgb.flds)+len(ovgb.fns))
		for _, f := range *ovgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ovgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ovgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OtaVersionSelect is the builder for selecting fields of OtaVersion entities.
type OtaVersionSelect struct {
	*OtaVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ovs *OtaVersionSelect) Aggregate(fns ...AggregateFunc) *OtaVersionSelect {
	ovs.fns = append(ovs.fns, fns...)
	return ovs
}

// Scan applies the selector query and scans the result into the given value.
func (ovs *OtaVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ovs.ctx, ent.OpQuerySelect)
	if err := ovs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OtaVersionQuery, *OtaVersionSelect](ctx, ovs.OtaVersionQuery, ovs, ovs.inters, v)
}

func (ovs *OtaVersionSelect) sqlScan(ctx context.Context, root *OtaVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ovs.fns))
	for _, fn := range ovs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ovs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ovs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
