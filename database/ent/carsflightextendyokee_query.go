// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/jianbo-zh/jydata/database/ent/carsflightextendyokee"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CarsFlightExtendYokeeQuery is the builder for querying CarsFlightExtendYokee entities.
type CarsFlightExtendYokeeQuery struct {
	config
	ctx        *QueryContext
	order      []carsflightextendyokee.OrderOption
	inters     []Interceptor
	predicates []predicate.CarsFlightExtendYokee
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarsFlightExtendYokeeQuery builder.
func (cfeyq *CarsFlightExtendYokeeQuery) Where(ps ...predicate.CarsFlightExtendYokee) *CarsFlightExtendYokeeQuery {
	cfeyq.predicates = append(cfeyq.predicates, ps...)
	return cfeyq
}

// Limit the number of records to be returned by this query.
func (cfeyq *CarsFlightExtendYokeeQuery) Limit(limit int) *CarsFlightExtendYokeeQuery {
	cfeyq.ctx.Limit = &limit
	return cfeyq
}

// Offset to start from.
func (cfeyq *CarsFlightExtendYokeeQuery) Offset(offset int) *CarsFlightExtendYokeeQuery {
	cfeyq.ctx.Offset = &offset
	return cfeyq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cfeyq *CarsFlightExtendYokeeQuery) Unique(unique bool) *CarsFlightExtendYokeeQuery {
	cfeyq.ctx.Unique = &unique
	return cfeyq
}

// Order specifies how the records should be ordered.
func (cfeyq *CarsFlightExtendYokeeQuery) Order(o ...carsflightextendyokee.OrderOption) *CarsFlightExtendYokeeQuery {
	cfeyq.order = append(cfeyq.order, o...)
	return cfeyq
}

// First returns the first CarsFlightExtendYokee entity from the query.
// Returns a *NotFoundError when no CarsFlightExtendYokee was found.
func (cfeyq *CarsFlightExtendYokeeQuery) First(ctx context.Context) (*CarsFlightExtendYokee, error) {
	nodes, err := cfeyq.Limit(1).All(setContextOp(ctx, cfeyq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{carsflightextendyokee.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cfeyq *CarsFlightExtendYokeeQuery) FirstX(ctx context.Context) *CarsFlightExtendYokee {
	node, err := cfeyq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarsFlightExtendYokee ID from the query.
// Returns a *NotFoundError when no CarsFlightExtendYokee ID was found.
func (cfeyq *CarsFlightExtendYokeeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfeyq.Limit(1).IDs(setContextOp(ctx, cfeyq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{carsflightextendyokee.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cfeyq *CarsFlightExtendYokeeQuery) FirstIDX(ctx context.Context) int {
	id, err := cfeyq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarsFlightExtendYokee entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarsFlightExtendYokee entity is found.
// Returns a *NotFoundError when no CarsFlightExtendYokee entities are found.
func (cfeyq *CarsFlightExtendYokeeQuery) Only(ctx context.Context) (*CarsFlightExtendYokee, error) {
	nodes, err := cfeyq.Limit(2).All(setContextOp(ctx, cfeyq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{carsflightextendyokee.Label}
	default:
		return nil, &NotSingularError{carsflightextendyokee.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cfeyq *CarsFlightExtendYokeeQuery) OnlyX(ctx context.Context) *CarsFlightExtendYokee {
	node, err := cfeyq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarsFlightExtendYokee ID in the query.
// Returns a *NotSingularError when more than one CarsFlightExtendYokee ID is found.
// Returns a *NotFoundError when no entities are found.
func (cfeyq *CarsFlightExtendYokeeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfeyq.Limit(2).IDs(setContextOp(ctx, cfeyq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{carsflightextendyokee.Label}
	default:
		err = &NotSingularError{carsflightextendyokee.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cfeyq *CarsFlightExtendYokeeQuery) OnlyIDX(ctx context.Context) int {
	id, err := cfeyq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarsFlightExtendYokees.
func (cfeyq *CarsFlightExtendYokeeQuery) All(ctx context.Context) ([]*CarsFlightExtendYokee, error) {
	ctx = setContextOp(ctx, cfeyq.ctx, ent.OpQueryAll)
	if err := cfeyq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarsFlightExtendYokee, *CarsFlightExtendYokeeQuery]()
	return withInterceptors[[]*CarsFlightExtendYokee](ctx, cfeyq, qr, cfeyq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cfeyq *CarsFlightExtendYokeeQuery) AllX(ctx context.Context) []*CarsFlightExtendYokee {
	nodes, err := cfeyq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarsFlightExtendYokee IDs.
func (cfeyq *CarsFlightExtendYokeeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if cfeyq.ctx.Unique == nil && cfeyq.path != nil {
		cfeyq.Unique(true)
	}
	ctx = setContextOp(ctx, cfeyq.ctx, ent.OpQueryIDs)
	if err = cfeyq.Select(carsflightextendyokee.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cfeyq *CarsFlightExtendYokeeQuery) IDsX(ctx context.Context) []int {
	ids, err := cfeyq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cfeyq *CarsFlightExtendYokeeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cfeyq.ctx, ent.OpQueryCount)
	if err := cfeyq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cfeyq, querierCount[*CarsFlightExtendYokeeQuery](), cfeyq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cfeyq *CarsFlightExtendYokeeQuery) CountX(ctx context.Context) int {
	count, err := cfeyq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cfeyq *CarsFlightExtendYokeeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cfeyq.ctx, ent.OpQueryExist)
	switch _, err := cfeyq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cfeyq *CarsFlightExtendYokeeQuery) ExistX(ctx context.Context) bool {
	exist, err := cfeyq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarsFlightExtendYokeeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cfeyq *CarsFlightExtendYokeeQuery) Clone() *CarsFlightExtendYokeeQuery {
	if cfeyq == nil {
		return nil
	}
	return &CarsFlightExtendYokeeQuery{
		config:     cfeyq.config,
		ctx:        cfeyq.ctx.Clone(),
		order:      append([]carsflightextendyokee.OrderOption{}, cfeyq.order...),
		inters:     append([]Interceptor{}, cfeyq.inters...),
		predicates: append([]predicate.CarsFlightExtendYokee{}, cfeyq.predicates...),
		// clone intermediate query.
		sql:  cfeyq.sql.Clone(),
		path: cfeyq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FlightID int `json:"flight_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarsFlightExtendYokee.Query().
//		GroupBy(carsflightextendyokee.FieldFlightID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cfeyq *CarsFlightExtendYokeeQuery) GroupBy(field string, fields ...string) *CarsFlightExtendYokeeGroupBy {
	cfeyq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarsFlightExtendYokeeGroupBy{build: cfeyq}
	grbuild.flds = &cfeyq.ctx.Fields
	grbuild.label = carsflightextendyokee.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FlightID int `json:"flight_id,omitempty"`
//	}
//
//	client.CarsFlightExtendYokee.Query().
//		Select(carsflightextendyokee.FieldFlightID).
//		Scan(ctx, &v)
func (cfeyq *CarsFlightExtendYokeeQuery) Select(fields ...string) *CarsFlightExtendYokeeSelect {
	cfeyq.ctx.Fields = append(cfeyq.ctx.Fields, fields...)
	sbuild := &CarsFlightExtendYokeeSelect{CarsFlightExtendYokeeQuery: cfeyq}
	sbuild.label = carsflightextendyokee.Label
	sbuild.flds, sbuild.scan = &cfeyq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarsFlightExtendYokeeSelect configured with the given aggregations.
func (cfeyq *CarsFlightExtendYokeeQuery) Aggregate(fns ...AggregateFunc) *CarsFlightExtendYokeeSelect {
	return cfeyq.Select().Aggregate(fns...)
}

func (cfeyq *CarsFlightExtendYokeeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cfeyq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cfeyq); err != nil {
				return err
			}
		}
	}
	for _, f := range cfeyq.ctx.Fields {
		if !carsflightextendyokee.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cfeyq.path != nil {
		prev, err := cfeyq.path(ctx)
		if err != nil {
			return err
		}
		cfeyq.sql = prev
	}
	return nil
}

func (cfeyq *CarsFlightExtendYokeeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarsFlightExtendYokee, error) {
	var (
		nodes = []*CarsFlightExtendYokee{}
		_spec = cfeyq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarsFlightExtendYokee).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarsFlightExtendYokee{config: cfeyq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cfeyq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (cfeyq *CarsFlightExtendYokeeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cfeyq.querySpec()
	_spec.Node.Columns = cfeyq.ctx.Fields
	if len(cfeyq.ctx.Fields) > 0 {
		_spec.Unique = cfeyq.ctx.Unique != nil && *cfeyq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cfeyq.driver, _spec)
}

func (cfeyq *CarsFlightExtendYokeeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(carsflightextendyokee.Table, carsflightextendyokee.Columns, sqlgraph.NewFieldSpec(carsflightextendyokee.FieldID, field.TypeInt))
	_spec.From = cfeyq.sql
	if unique := cfeyq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cfeyq.path != nil {
		_spec.Unique = true
	}
	if fields := cfeyq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, carsflightextendyokee.FieldID)
		for i := range fields {
			if fields[i] != carsflightextendyokee.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cfeyq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cfeyq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cfeyq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cfeyq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cfeyq *CarsFlightExtendYokeeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cfeyq.driver.Dialect())
	t1 := builder.Table(carsflightextendyokee.Table)
	columns := cfeyq.ctx.Fields
	if len(columns) == 0 {
		columns = carsflightextendyokee.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cfeyq.sql != nil {
		selector = cfeyq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cfeyq.ctx.Unique != nil && *cfeyq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cfeyq.predicates {
		p(selector)
	}
	for _, p := range cfeyq.order {
		p(selector)
	}
	if offset := cfeyq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cfeyq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CarsFlightExtendYokeeGroupBy is the group-by builder for CarsFlightExtendYokee entities.
type CarsFlightExtendYokeeGroupBy struct {
	selector
	build *CarsFlightExtendYokeeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cfeygb *CarsFlightExtendYokeeGroupBy) Aggregate(fns ...AggregateFunc) *CarsFlightExtendYokeeGroupBy {
	cfeygb.fns = append(cfeygb.fns, fns...)
	return cfeygb
}

// Scan applies the selector query and scans the result into the given value.
func (cfeygb *CarsFlightExtendYokeeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cfeygb.build.ctx, ent.OpQueryGroupBy)
	if err := cfeygb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarsFlightExtendYokeeQuery, *CarsFlightExtendYokeeGroupBy](ctx, cfeygb.build, cfeygb, cfeygb.build.inters, v)
}

func (cfeygb *CarsFlightExtendYokeeGroupBy) sqlScan(ctx context.Context, root *CarsFlightExtendYokeeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cfeygb.fns))
	for _, fn := range cfeygb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cfeygb.flds)+len(cfeygb.fns))
		for _, f := range *cfeygb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cfeygb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfeygb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarsFlightExtendYokeeSelect is the builder for selecting fields of CarsFlightExtendYokee entities.
type CarsFlightExtendYokeeSelect struct {
	*CarsFlightExtendYokeeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cfeys *CarsFlightExtendYokeeSelect) Aggregate(fns ...AggregateFunc) *CarsFlightExtendYokeeSelect {
	cfeys.fns = append(cfeys.fns, fns...)
	return cfeys
}

// Scan applies the selector query and scans the result into the given value.
func (cfeys *CarsFlightExtendYokeeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cfeys.ctx, ent.OpQuerySelect)
	if err := cfeys.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarsFlightExtendYokeeQuery, *CarsFlightExtendYokeeSelect](ctx, cfeys.CarsFlightExtendYokeeQuery, cfeys, cfeys.inters, v)
}

func (cfeys *CarsFlightExtendYokeeSelect) sqlScan(ctx context.Context, root *CarsFlightExtendYokeeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cfeys.fns))
	for _, fn := range cfeys.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cfeys.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfeys.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
