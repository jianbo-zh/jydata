// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"github.com/jianbo-zh/jydata/database/ent/schetask"
	"github.com/jianbo-zh/jydata/database/ent/schetaskevent"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ScheTaskEventQuery is the builder for querying ScheTaskEvent entities.
type ScheTaskEventQuery struct {
	config
	ctx          *QueryContext
	order        []schetaskevent.OrderOption
	inters       []Interceptor
	predicates   []predicate.ScheTaskEvent
	withScheTask *ScheTaskQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ScheTaskEventQuery builder.
func (steq *ScheTaskEventQuery) Where(ps ...predicate.ScheTaskEvent) *ScheTaskEventQuery {
	steq.predicates = append(steq.predicates, ps...)
	return steq
}

// Limit the number of records to be returned by this query.
func (steq *ScheTaskEventQuery) Limit(limit int) *ScheTaskEventQuery {
	steq.ctx.Limit = &limit
	return steq
}

// Offset to start from.
func (steq *ScheTaskEventQuery) Offset(offset int) *ScheTaskEventQuery {
	steq.ctx.Offset = &offset
	return steq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (steq *ScheTaskEventQuery) Unique(unique bool) *ScheTaskEventQuery {
	steq.ctx.Unique = &unique
	return steq
}

// Order specifies how the records should be ordered.
func (steq *ScheTaskEventQuery) Order(o ...schetaskevent.OrderOption) *ScheTaskEventQuery {
	steq.order = append(steq.order, o...)
	return steq
}

// QueryScheTask chains the current query on the "sche_task" edge.
func (steq *ScheTaskEventQuery) QueryScheTask() *ScheTaskQuery {
	query := (&ScheTaskClient{config: steq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := steq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := steq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(schetaskevent.Table, schetaskevent.FieldID, selector),
			sqlgraph.To(schetask.Table, schetask.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, schetaskevent.ScheTaskTable, schetaskevent.ScheTaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(steq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ScheTaskEvent entity from the query.
// Returns a *NotFoundError when no ScheTaskEvent was found.
func (steq *ScheTaskEventQuery) First(ctx context.Context) (*ScheTaskEvent, error) {
	nodes, err := steq.Limit(1).All(setContextOp(ctx, steq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{schetaskevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (steq *ScheTaskEventQuery) FirstX(ctx context.Context) *ScheTaskEvent {
	node, err := steq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ScheTaskEvent ID from the query.
// Returns a *NotFoundError when no ScheTaskEvent ID was found.
func (steq *ScheTaskEventQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = steq.Limit(1).IDs(setContextOp(ctx, steq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{schetaskevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (steq *ScheTaskEventQuery) FirstIDX(ctx context.Context) int {
	id, err := steq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ScheTaskEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ScheTaskEvent entity is found.
// Returns a *NotFoundError when no ScheTaskEvent entities are found.
func (steq *ScheTaskEventQuery) Only(ctx context.Context) (*ScheTaskEvent, error) {
	nodes, err := steq.Limit(2).All(setContextOp(ctx, steq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{schetaskevent.Label}
	default:
		return nil, &NotSingularError{schetaskevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (steq *ScheTaskEventQuery) OnlyX(ctx context.Context) *ScheTaskEvent {
	node, err := steq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ScheTaskEvent ID in the query.
// Returns a *NotSingularError when more than one ScheTaskEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (steq *ScheTaskEventQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = steq.Limit(2).IDs(setContextOp(ctx, steq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{schetaskevent.Label}
	default:
		err = &NotSingularError{schetaskevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (steq *ScheTaskEventQuery) OnlyIDX(ctx context.Context) int {
	id, err := steq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ScheTaskEvents.
func (steq *ScheTaskEventQuery) All(ctx context.Context) ([]*ScheTaskEvent, error) {
	ctx = setContextOp(ctx, steq.ctx, ent.OpQueryAll)
	if err := steq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ScheTaskEvent, *ScheTaskEventQuery]()
	return withInterceptors[[]*ScheTaskEvent](ctx, steq, qr, steq.inters)
}

// AllX is like All, but panics if an error occurs.
func (steq *ScheTaskEventQuery) AllX(ctx context.Context) []*ScheTaskEvent {
	nodes, err := steq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ScheTaskEvent IDs.
func (steq *ScheTaskEventQuery) IDs(ctx context.Context) (ids []int, err error) {
	if steq.ctx.Unique == nil && steq.path != nil {
		steq.Unique(true)
	}
	ctx = setContextOp(ctx, steq.ctx, ent.OpQueryIDs)
	if err = steq.Select(schetaskevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (steq *ScheTaskEventQuery) IDsX(ctx context.Context) []int {
	ids, err := steq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (steq *ScheTaskEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, steq.ctx, ent.OpQueryCount)
	if err := steq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, steq, querierCount[*ScheTaskEventQuery](), steq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (steq *ScheTaskEventQuery) CountX(ctx context.Context) int {
	count, err := steq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (steq *ScheTaskEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, steq.ctx, ent.OpQueryExist)
	switch _, err := steq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (steq *ScheTaskEventQuery) ExistX(ctx context.Context) bool {
	exist, err := steq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ScheTaskEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (steq *ScheTaskEventQuery) Clone() *ScheTaskEventQuery {
	if steq == nil {
		return nil
	}
	return &ScheTaskEventQuery{
		config:       steq.config,
		ctx:          steq.ctx.Clone(),
		order:        append([]schetaskevent.OrderOption{}, steq.order...),
		inters:       append([]Interceptor{}, steq.inters...),
		predicates:   append([]predicate.ScheTaskEvent{}, steq.predicates...),
		withScheTask: steq.withScheTask.Clone(),
		// clone intermediate query.
		sql:  steq.sql.Clone(),
		path: steq.path,
	}
}

// WithScheTask tells the query-builder to eager-load the nodes that are connected to
// the "sche_task" edge. The optional arguments are used to configure the query builder of the edge.
func (steq *ScheTaskEventQuery) WithScheTask(opts ...func(*ScheTaskQuery)) *ScheTaskEventQuery {
	query := (&ScheTaskClient{config: steq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	steq.withScheTask = query
	return steq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScheTaskID int `json:"sche_task_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ScheTaskEvent.Query().
//		GroupBy(schetaskevent.FieldScheTaskID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (steq *ScheTaskEventQuery) GroupBy(field string, fields ...string) *ScheTaskEventGroupBy {
	steq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ScheTaskEventGroupBy{build: steq}
	grbuild.flds = &steq.ctx.Fields
	grbuild.label = schetaskevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScheTaskID int `json:"sche_task_id,omitempty"`
//	}
//
//	client.ScheTaskEvent.Query().
//		Select(schetaskevent.FieldScheTaskID).
//		Scan(ctx, &v)
func (steq *ScheTaskEventQuery) Select(fields ...string) *ScheTaskEventSelect {
	steq.ctx.Fields = append(steq.ctx.Fields, fields...)
	sbuild := &ScheTaskEventSelect{ScheTaskEventQuery: steq}
	sbuild.label = schetaskevent.Label
	sbuild.flds, sbuild.scan = &steq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ScheTaskEventSelect configured with the given aggregations.
func (steq *ScheTaskEventQuery) Aggregate(fns ...AggregateFunc) *ScheTaskEventSelect {
	return steq.Select().Aggregate(fns...)
}

func (steq *ScheTaskEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range steq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, steq); err != nil {
				return err
			}
		}
	}
	for _, f := range steq.ctx.Fields {
		if !schetaskevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if steq.path != nil {
		prev, err := steq.path(ctx)
		if err != nil {
			return err
		}
		steq.sql = prev
	}
	return nil
}

func (steq *ScheTaskEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ScheTaskEvent, error) {
	var (
		nodes       = []*ScheTaskEvent{}
		_spec       = steq.querySpec()
		loadedTypes = [1]bool{
			steq.withScheTask != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ScheTaskEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ScheTaskEvent{config: steq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, steq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := steq.withScheTask; query != nil {
		if err := steq.loadScheTask(ctx, query, nodes, nil,
			func(n *ScheTaskEvent, e *ScheTask) { n.Edges.ScheTask = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (steq *ScheTaskEventQuery) loadScheTask(ctx context.Context, query *ScheTaskQuery, nodes []*ScheTaskEvent, init func(*ScheTaskEvent), assign func(*ScheTaskEvent, *ScheTask)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ScheTaskEvent)
	for i := range nodes {
		fk := nodes[i].ScheTaskID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(schetask.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "sche_task_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (steq *ScheTaskEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := steq.querySpec()
	_spec.Node.Columns = steq.ctx.Fields
	if len(steq.ctx.Fields) > 0 {
		_spec.Unique = steq.ctx.Unique != nil && *steq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, steq.driver, _spec)
}

func (steq *ScheTaskEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(schetaskevent.Table, schetaskevent.Columns, sqlgraph.NewFieldSpec(schetaskevent.FieldID, field.TypeInt))
	_spec.From = steq.sql
	if unique := steq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if steq.path != nil {
		_spec.Unique = true
	}
	if fields := steq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, schetaskevent.FieldID)
		for i := range fields {
			if fields[i] != schetaskevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if steq.withScheTask != nil {
			_spec.Node.AddColumnOnce(schetaskevent.FieldScheTaskID)
		}
	}
	if ps := steq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := steq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := steq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := steq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (steq *ScheTaskEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(steq.driver.Dialect())
	t1 := builder.Table(schetaskevent.Table)
	columns := steq.ctx.Fields
	if len(columns) == 0 {
		columns = schetaskevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if steq.sql != nil {
		selector = steq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if steq.ctx.Unique != nil && *steq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range steq.predicates {
		p(selector)
	}
	for _, p := range steq.order {
		p(selector)
	}
	if offset := steq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := steq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ScheTaskEventGroupBy is the group-by builder for ScheTaskEvent entities.
type ScheTaskEventGroupBy struct {
	selector
	build *ScheTaskEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (stegb *ScheTaskEventGroupBy) Aggregate(fns ...AggregateFunc) *ScheTaskEventGroupBy {
	stegb.fns = append(stegb.fns, fns...)
	return stegb
}

// Scan applies the selector query and scans the result into the given value.
func (stegb *ScheTaskEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, stegb.build.ctx, ent.OpQueryGroupBy)
	if err := stegb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ScheTaskEventQuery, *ScheTaskEventGroupBy](ctx, stegb.build, stegb, stegb.build.inters, v)
}

func (stegb *ScheTaskEventGroupBy) sqlScan(ctx context.Context, root *ScheTaskEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(stegb.fns))
	for _, fn := range stegb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*stegb.flds)+len(stegb.fns))
		for _, f := range *stegb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*stegb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := stegb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ScheTaskEventSelect is the builder for selecting fields of ScheTaskEvent entities.
type ScheTaskEventSelect struct {
	*ScheTaskEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (stes *ScheTaskEventSelect) Aggregate(fns ...AggregateFunc) *ScheTaskEventSelect {
	stes.fns = append(stes.fns, fns...)
	return stes
}

// Scan applies the selector query and scans the result into the given value.
func (stes *ScheTaskEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, stes.ctx, ent.OpQuerySelect)
	if err := stes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ScheTaskEventQuery, *ScheTaskEventSelect](ctx, stes.ScheTaskEventQuery, stes, stes.inters, v)
}

func (stes *ScheTaskEventSelect) sqlScan(ctx context.Context, root *ScheTaskEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(stes.fns))
	for _, fn := range stes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*stes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := stes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
