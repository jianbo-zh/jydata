// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jianbo-zh/jydata/database/ent/caralarm"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
)

// CarAlarmQuery is the builder for querying CarAlarm entities.
type CarAlarmQuery struct {
	config
	ctx        *QueryContext
	order      []caralarm.OrderOption
	inters     []Interceptor
	predicates []predicate.CarAlarm
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CarAlarmQuery builder.
func (caq *CarAlarmQuery) Where(ps ...predicate.CarAlarm) *CarAlarmQuery {
	caq.predicates = append(caq.predicates, ps...)
	return caq
}

// Limit the number of records to be returned by this query.
func (caq *CarAlarmQuery) Limit(limit int) *CarAlarmQuery {
	caq.ctx.Limit = &limit
	return caq
}

// Offset to start from.
func (caq *CarAlarmQuery) Offset(offset int) *CarAlarmQuery {
	caq.ctx.Offset = &offset
	return caq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (caq *CarAlarmQuery) Unique(unique bool) *CarAlarmQuery {
	caq.ctx.Unique = &unique
	return caq
}

// Order specifies how the records should be ordered.
func (caq *CarAlarmQuery) Order(o ...caralarm.OrderOption) *CarAlarmQuery {
	caq.order = append(caq.order, o...)
	return caq
}

// First returns the first CarAlarm entity from the query.
// Returns a *NotFoundError when no CarAlarm was found.
func (caq *CarAlarmQuery) First(ctx context.Context) (*CarAlarm, error) {
	nodes, err := caq.Limit(1).All(setContextOp(ctx, caq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{caralarm.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (caq *CarAlarmQuery) FirstX(ctx context.Context) *CarAlarm {
	node, err := caq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CarAlarm ID from the query.
// Returns a *NotFoundError when no CarAlarm ID was found.
func (caq *CarAlarmQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = caq.Limit(1).IDs(setContextOp(ctx, caq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{caralarm.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (caq *CarAlarmQuery) FirstIDX(ctx context.Context) int {
	id, err := caq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CarAlarm entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CarAlarm entity is found.
// Returns a *NotFoundError when no CarAlarm entities are found.
func (caq *CarAlarmQuery) Only(ctx context.Context) (*CarAlarm, error) {
	nodes, err := caq.Limit(2).All(setContextOp(ctx, caq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{caralarm.Label}
	default:
		return nil, &NotSingularError{caralarm.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (caq *CarAlarmQuery) OnlyX(ctx context.Context) *CarAlarm {
	node, err := caq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CarAlarm ID in the query.
// Returns a *NotSingularError when more than one CarAlarm ID is found.
// Returns a *NotFoundError when no entities are found.
func (caq *CarAlarmQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = caq.Limit(2).IDs(setContextOp(ctx, caq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{caralarm.Label}
	default:
		err = &NotSingularError{caralarm.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (caq *CarAlarmQuery) OnlyIDX(ctx context.Context) int {
	id, err := caq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CarAlarms.
func (caq *CarAlarmQuery) All(ctx context.Context) ([]*CarAlarm, error) {
	ctx = setContextOp(ctx, caq.ctx, ent.OpQueryAll)
	if err := caq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CarAlarm, *CarAlarmQuery]()
	return withInterceptors[[]*CarAlarm](ctx, caq, qr, caq.inters)
}

// AllX is like All, but panics if an error occurs.
func (caq *CarAlarmQuery) AllX(ctx context.Context) []*CarAlarm {
	nodes, err := caq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CarAlarm IDs.
func (caq *CarAlarmQuery) IDs(ctx context.Context) (ids []int, err error) {
	if caq.ctx.Unique == nil && caq.path != nil {
		caq.Unique(true)
	}
	ctx = setContextOp(ctx, caq.ctx, ent.OpQueryIDs)
	if err = caq.Select(caralarm.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (caq *CarAlarmQuery) IDsX(ctx context.Context) []int {
	ids, err := caq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (caq *CarAlarmQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, caq.ctx, ent.OpQueryCount)
	if err := caq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, caq, querierCount[*CarAlarmQuery](), caq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (caq *CarAlarmQuery) CountX(ctx context.Context) int {
	count, err := caq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (caq *CarAlarmQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, caq.ctx, ent.OpQueryExist)
	switch _, err := caq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (caq *CarAlarmQuery) ExistX(ctx context.Context) bool {
	exist, err := caq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CarAlarmQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (caq *CarAlarmQuery) Clone() *CarAlarmQuery {
	if caq == nil {
		return nil
	}
	return &CarAlarmQuery{
		config:     caq.config,
		ctx:        caq.ctx.Clone(),
		order:      append([]caralarm.OrderOption{}, caq.order...),
		inters:     append([]Interceptor{}, caq.inters...),
		predicates: append([]predicate.CarAlarm{}, caq.predicates...),
		// clone intermediate query.
		sql:  caq.sql.Clone(),
		path: caq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeviceID string `json:"device_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CarAlarm.Query().
//		GroupBy(caralarm.FieldDeviceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (caq *CarAlarmQuery) GroupBy(field string, fields ...string) *CarAlarmGroupBy {
	caq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CarAlarmGroupBy{build: caq}
	grbuild.flds = &caq.ctx.Fields
	grbuild.label = caralarm.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeviceID string `json:"device_id,omitempty"`
//	}
//
//	client.CarAlarm.Query().
//		Select(caralarm.FieldDeviceID).
//		Scan(ctx, &v)
func (caq *CarAlarmQuery) Select(fields ...string) *CarAlarmSelect {
	caq.ctx.Fields = append(caq.ctx.Fields, fields...)
	sbuild := &CarAlarmSelect{CarAlarmQuery: caq}
	sbuild.label = caralarm.Label
	sbuild.flds, sbuild.scan = &caq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CarAlarmSelect configured with the given aggregations.
func (caq *CarAlarmQuery) Aggregate(fns ...AggregateFunc) *CarAlarmSelect {
	return caq.Select().Aggregate(fns...)
}

func (caq *CarAlarmQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range caq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, caq); err != nil {
				return err
			}
		}
	}
	for _, f := range caq.ctx.Fields {
		if !caralarm.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if caq.path != nil {
		prev, err := caq.path(ctx)
		if err != nil {
			return err
		}
		caq.sql = prev
	}
	return nil
}

func (caq *CarAlarmQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CarAlarm, error) {
	var (
		nodes = []*CarAlarm{}
		_spec = caq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CarAlarm).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CarAlarm{config: caq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, caq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (caq *CarAlarmQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := caq.querySpec()
	_spec.Node.Columns = caq.ctx.Fields
	if len(caq.ctx.Fields) > 0 {
		_spec.Unique = caq.ctx.Unique != nil && *caq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, caq.driver, _spec)
}

func (caq *CarAlarmQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(caralarm.Table, caralarm.Columns, sqlgraph.NewFieldSpec(caralarm.FieldID, field.TypeInt))
	_spec.From = caq.sql
	if unique := caq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if caq.path != nil {
		_spec.Unique = true
	}
	if fields := caq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, caralarm.FieldID)
		for i := range fields {
			if fields[i] != caralarm.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := caq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := caq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := caq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := caq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (caq *CarAlarmQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(caq.driver.Dialect())
	t1 := builder.Table(caralarm.Table)
	columns := caq.ctx.Fields
	if len(columns) == 0 {
		columns = caralarm.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if caq.sql != nil {
		selector = caq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if caq.ctx.Unique != nil && *caq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range caq.predicates {
		p(selector)
	}
	for _, p := range caq.order {
		p(selector)
	}
	if offset := caq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := caq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CarAlarmGroupBy is the group-by builder for CarAlarm entities.
type CarAlarmGroupBy struct {
	selector
	build *CarAlarmQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cagb *CarAlarmGroupBy) Aggregate(fns ...AggregateFunc) *CarAlarmGroupBy {
	cagb.fns = append(cagb.fns, fns...)
	return cagb
}

// Scan applies the selector query and scans the result into the given value.
func (cagb *CarAlarmGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cagb.build.ctx, ent.OpQueryGroupBy)
	if err := cagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarAlarmQuery, *CarAlarmGroupBy](ctx, cagb.build, cagb, cagb.build.inters, v)
}

func (cagb *CarAlarmGroupBy) sqlScan(ctx context.Context, root *CarAlarmQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cagb.fns))
	for _, fn := range cagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cagb.flds)+len(cagb.fns))
		for _, f := range *cagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CarAlarmSelect is the builder for selecting fields of CarAlarm entities.
type CarAlarmSelect struct {
	*CarAlarmQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cas *CarAlarmSelect) Aggregate(fns ...AggregateFunc) *CarAlarmSelect {
	cas.fns = append(cas.fns, fns...)
	return cas
}

// Scan applies the selector query and scans the result into the given value.
func (cas *CarAlarmSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cas.ctx, ent.OpQuerySelect)
	if err := cas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CarAlarmQuery, *CarAlarmSelect](ctx, cas.CarAlarmQuery, cas, cas.inters, v)
}

func (cas *CarAlarmSelect) sqlScan(ctx context.Context, root *CarAlarmQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cas.fns))
	for _, fn := range cas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
