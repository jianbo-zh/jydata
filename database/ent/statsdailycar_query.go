// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"github.com/jianbo-zh/jydata/database/ent/statsdailycar"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// StatsDailyCarQuery is the builder for querying StatsDailyCar entities.
type StatsDailyCarQuery struct {
	config
	ctx        *QueryContext
	order      []statsdailycar.OrderOption
	inters     []Interceptor
	predicates []predicate.StatsDailyCar
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StatsDailyCarQuery builder.
func (sdcq *StatsDailyCarQuery) Where(ps ...predicate.StatsDailyCar) *StatsDailyCarQuery {
	sdcq.predicates = append(sdcq.predicates, ps...)
	return sdcq
}

// Limit the number of records to be returned by this query.
func (sdcq *StatsDailyCarQuery) Limit(limit int) *StatsDailyCarQuery {
	sdcq.ctx.Limit = &limit
	return sdcq
}

// Offset to start from.
func (sdcq *StatsDailyCarQuery) Offset(offset int) *StatsDailyCarQuery {
	sdcq.ctx.Offset = &offset
	return sdcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sdcq *StatsDailyCarQuery) Unique(unique bool) *StatsDailyCarQuery {
	sdcq.ctx.Unique = &unique
	return sdcq
}

// Order specifies how the records should be ordered.
func (sdcq *StatsDailyCarQuery) Order(o ...statsdailycar.OrderOption) *StatsDailyCarQuery {
	sdcq.order = append(sdcq.order, o...)
	return sdcq
}

// First returns the first StatsDailyCar entity from the query.
// Returns a *NotFoundError when no StatsDailyCar was found.
func (sdcq *StatsDailyCarQuery) First(ctx context.Context) (*StatsDailyCar, error) {
	nodes, err := sdcq.Limit(1).All(setContextOp(ctx, sdcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{statsdailycar.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sdcq *StatsDailyCarQuery) FirstX(ctx context.Context) *StatsDailyCar {
	node, err := sdcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StatsDailyCar ID from the query.
// Returns a *NotFoundError when no StatsDailyCar ID was found.
func (sdcq *StatsDailyCarQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sdcq.Limit(1).IDs(setContextOp(ctx, sdcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{statsdailycar.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sdcq *StatsDailyCarQuery) FirstIDX(ctx context.Context) int {
	id, err := sdcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StatsDailyCar entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StatsDailyCar entity is found.
// Returns a *NotFoundError when no StatsDailyCar entities are found.
func (sdcq *StatsDailyCarQuery) Only(ctx context.Context) (*StatsDailyCar, error) {
	nodes, err := sdcq.Limit(2).All(setContextOp(ctx, sdcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{statsdailycar.Label}
	default:
		return nil, &NotSingularError{statsdailycar.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sdcq *StatsDailyCarQuery) OnlyX(ctx context.Context) *StatsDailyCar {
	node, err := sdcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StatsDailyCar ID in the query.
// Returns a *NotSingularError when more than one StatsDailyCar ID is found.
// Returns a *NotFoundError when no entities are found.
func (sdcq *StatsDailyCarQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sdcq.Limit(2).IDs(setContextOp(ctx, sdcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{statsdailycar.Label}
	default:
		err = &NotSingularError{statsdailycar.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sdcq *StatsDailyCarQuery) OnlyIDX(ctx context.Context) int {
	id, err := sdcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StatsDailyCars.
func (sdcq *StatsDailyCarQuery) All(ctx context.Context) ([]*StatsDailyCar, error) {
	ctx = setContextOp(ctx, sdcq.ctx, ent.OpQueryAll)
	if err := sdcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StatsDailyCar, *StatsDailyCarQuery]()
	return withInterceptors[[]*StatsDailyCar](ctx, sdcq, qr, sdcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sdcq *StatsDailyCarQuery) AllX(ctx context.Context) []*StatsDailyCar {
	nodes, err := sdcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StatsDailyCar IDs.
func (sdcq *StatsDailyCarQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sdcq.ctx.Unique == nil && sdcq.path != nil {
		sdcq.Unique(true)
	}
	ctx = setContextOp(ctx, sdcq.ctx, ent.OpQueryIDs)
	if err = sdcq.Select(statsdailycar.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sdcq *StatsDailyCarQuery) IDsX(ctx context.Context) []int {
	ids, err := sdcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sdcq *StatsDailyCarQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sdcq.ctx, ent.OpQueryCount)
	if err := sdcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sdcq, querierCount[*StatsDailyCarQuery](), sdcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sdcq *StatsDailyCarQuery) CountX(ctx context.Context) int {
	count, err := sdcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sdcq *StatsDailyCarQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sdcq.ctx, ent.OpQueryExist)
	switch _, err := sdcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sdcq *StatsDailyCarQuery) ExistX(ctx context.Context) bool {
	exist, err := sdcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StatsDailyCarQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sdcq *StatsDailyCarQuery) Clone() *StatsDailyCarQuery {
	if sdcq == nil {
		return nil
	}
	return &StatsDailyCarQuery{
		config:     sdcq.config,
		ctx:        sdcq.ctx.Clone(),
		order:      append([]statsdailycar.OrderOption{}, sdcq.order...),
		inters:     append([]Interceptor{}, sdcq.inters...),
		predicates: append([]predicate.StatsDailyCar{}, sdcq.predicates...),
		// clone intermediate query.
		sql:  sdcq.sql.Clone(),
		path: sdcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StatsDailyCar.Query().
//		GroupBy(statsdailycar.FieldScenicAreaID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sdcq *StatsDailyCarQuery) GroupBy(field string, fields ...string) *StatsDailyCarGroupBy {
	sdcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StatsDailyCarGroupBy{build: sdcq}
	grbuild.flds = &sdcq.ctx.Fields
	grbuild.label = statsdailycar.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//	}
//
//	client.StatsDailyCar.Query().
//		Select(statsdailycar.FieldScenicAreaID).
//		Scan(ctx, &v)
func (sdcq *StatsDailyCarQuery) Select(fields ...string) *StatsDailyCarSelect {
	sdcq.ctx.Fields = append(sdcq.ctx.Fields, fields...)
	sbuild := &StatsDailyCarSelect{StatsDailyCarQuery: sdcq}
	sbuild.label = statsdailycar.Label
	sbuild.flds, sbuild.scan = &sdcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StatsDailyCarSelect configured with the given aggregations.
func (sdcq *StatsDailyCarQuery) Aggregate(fns ...AggregateFunc) *StatsDailyCarSelect {
	return sdcq.Select().Aggregate(fns...)
}

func (sdcq *StatsDailyCarQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sdcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sdcq); err != nil {
				return err
			}
		}
	}
	for _, f := range sdcq.ctx.Fields {
		if !statsdailycar.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sdcq.path != nil {
		prev, err := sdcq.path(ctx)
		if err != nil {
			return err
		}
		sdcq.sql = prev
	}
	return nil
}

func (sdcq *StatsDailyCarQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StatsDailyCar, error) {
	var (
		nodes = []*StatsDailyCar{}
		_spec = sdcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StatsDailyCar).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StatsDailyCar{config: sdcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sdcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sdcq *StatsDailyCarQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sdcq.querySpec()
	_spec.Node.Columns = sdcq.ctx.Fields
	if len(sdcq.ctx.Fields) > 0 {
		_spec.Unique = sdcq.ctx.Unique != nil && *sdcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sdcq.driver, _spec)
}

func (sdcq *StatsDailyCarQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(statsdailycar.Table, statsdailycar.Columns, sqlgraph.NewFieldSpec(statsdailycar.FieldID, field.TypeInt))
	_spec.From = sdcq.sql
	if unique := sdcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sdcq.path != nil {
		_spec.Unique = true
	}
	if fields := sdcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, statsdailycar.FieldID)
		for i := range fields {
			if fields[i] != statsdailycar.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sdcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sdcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sdcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sdcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sdcq *StatsDailyCarQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sdcq.driver.Dialect())
	t1 := builder.Table(statsdailycar.Table)
	columns := sdcq.ctx.Fields
	if len(columns) == 0 {
		columns = statsdailycar.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sdcq.sql != nil {
		selector = sdcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sdcq.ctx.Unique != nil && *sdcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sdcq.predicates {
		p(selector)
	}
	for _, p := range sdcq.order {
		p(selector)
	}
	if offset := sdcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sdcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StatsDailyCarGroupBy is the group-by builder for StatsDailyCar entities.
type StatsDailyCarGroupBy struct {
	selector
	build *StatsDailyCarQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sdcgb *StatsDailyCarGroupBy) Aggregate(fns ...AggregateFunc) *StatsDailyCarGroupBy {
	sdcgb.fns = append(sdcgb.fns, fns...)
	return sdcgb
}

// Scan applies the selector query and scans the result into the given value.
func (sdcgb *StatsDailyCarGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sdcgb.build.ctx, ent.OpQueryGroupBy)
	if err := sdcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsDailyCarQuery, *StatsDailyCarGroupBy](ctx, sdcgb.build, sdcgb, sdcgb.build.inters, v)
}

func (sdcgb *StatsDailyCarGroupBy) sqlScan(ctx context.Context, root *StatsDailyCarQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sdcgb.fns))
	for _, fn := range sdcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sdcgb.flds)+len(sdcgb.fns))
		for _, f := range *sdcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sdcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sdcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StatsDailyCarSelect is the builder for selecting fields of StatsDailyCar entities.
type StatsDailyCarSelect struct {
	*StatsDailyCarQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sdcs *StatsDailyCarSelect) Aggregate(fns ...AggregateFunc) *StatsDailyCarSelect {
	sdcs.fns = append(sdcs.fns, fns...)
	return sdcs
}

// Scan applies the selector query and scans the result into the given value.
func (sdcs *StatsDailyCarSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sdcs.ctx, ent.OpQuerySelect)
	if err := sdcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsDailyCarQuery, *StatsDailyCarSelect](ctx, sdcs.StatsDailyCarQuery, sdcs, sdcs.inters, v)
}

func (sdcs *StatsDailyCarSelect) sqlScan(ctx context.Context, root *StatsDailyCarQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sdcs.fns))
	for _, fn := range sdcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sdcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sdcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
