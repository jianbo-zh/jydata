// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"github.com/jianbo-zh/jydata/database/ent/profitreceiver"
	"github.com/jianbo-zh/jydata/database/ent/scenicarea"
)

// ProfitReceiverQuery is the builder for querying ProfitReceiver entities.
type ProfitReceiverQuery struct {
	config
	ctx                      *QueryContext
	order                    []profitreceiver.OrderOption
	inters                   []Interceptor
	predicates               []predicate.ProfitReceiver
	withBackgroundScenicArea *ScenicAreaQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProfitReceiverQuery builder.
func (prq *ProfitReceiverQuery) Where(ps ...predicate.ProfitReceiver) *ProfitReceiverQuery {
	prq.predicates = append(prq.predicates, ps...)
	return prq
}

// Limit the number of records to be returned by this query.
func (prq *ProfitReceiverQuery) Limit(limit int) *ProfitReceiverQuery {
	prq.ctx.Limit = &limit
	return prq
}

// Offset to start from.
func (prq *ProfitReceiverQuery) Offset(offset int) *ProfitReceiverQuery {
	prq.ctx.Offset = &offset
	return prq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (prq *ProfitReceiverQuery) Unique(unique bool) *ProfitReceiverQuery {
	prq.ctx.Unique = &unique
	return prq
}

// Order specifies how the records should be ordered.
func (prq *ProfitReceiverQuery) Order(o ...profitreceiver.OrderOption) *ProfitReceiverQuery {
	prq.order = append(prq.order, o...)
	return prq
}

// QueryBackgroundScenicArea chains the current query on the "background_scenic_area" edge.
func (prq *ProfitReceiverQuery) QueryBackgroundScenicArea() *ScenicAreaQuery {
	query := (&ScenicAreaClient{config: prq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := prq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := prq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(profitreceiver.Table, profitreceiver.FieldID, selector),
			sqlgraph.To(scenicarea.Table, scenicarea.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, profitreceiver.BackgroundScenicAreaTable, profitreceiver.BackgroundScenicAreaColumn),
		)
		fromU = sqlgraph.SetNeighbors(prq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProfitReceiver entity from the query.
// Returns a *NotFoundError when no ProfitReceiver was found.
func (prq *ProfitReceiverQuery) First(ctx context.Context) (*ProfitReceiver, error) {
	nodes, err := prq.Limit(1).All(setContextOp(ctx, prq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{profitreceiver.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (prq *ProfitReceiverQuery) FirstX(ctx context.Context) *ProfitReceiver {
	node, err := prq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProfitReceiver ID from the query.
// Returns a *NotFoundError when no ProfitReceiver ID was found.
func (prq *ProfitReceiverQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = prq.Limit(1).IDs(setContextOp(ctx, prq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{profitreceiver.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (prq *ProfitReceiverQuery) FirstIDX(ctx context.Context) int {
	id, err := prq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProfitReceiver entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProfitReceiver entity is found.
// Returns a *NotFoundError when no ProfitReceiver entities are found.
func (prq *ProfitReceiverQuery) Only(ctx context.Context) (*ProfitReceiver, error) {
	nodes, err := prq.Limit(2).All(setContextOp(ctx, prq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{profitreceiver.Label}
	default:
		return nil, &NotSingularError{profitreceiver.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (prq *ProfitReceiverQuery) OnlyX(ctx context.Context) *ProfitReceiver {
	node, err := prq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProfitReceiver ID in the query.
// Returns a *NotSingularError when more than one ProfitReceiver ID is found.
// Returns a *NotFoundError when no entities are found.
func (prq *ProfitReceiverQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = prq.Limit(2).IDs(setContextOp(ctx, prq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{profitreceiver.Label}
	default:
		err = &NotSingularError{profitreceiver.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (prq *ProfitReceiverQuery) OnlyIDX(ctx context.Context) int {
	id, err := prq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProfitReceivers.
func (prq *ProfitReceiverQuery) All(ctx context.Context) ([]*ProfitReceiver, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryAll)
	if err := prq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProfitReceiver, *ProfitReceiverQuery]()
	return withInterceptors[[]*ProfitReceiver](ctx, prq, qr, prq.inters)
}

// AllX is like All, but panics if an error occurs.
func (prq *ProfitReceiverQuery) AllX(ctx context.Context) []*ProfitReceiver {
	nodes, err := prq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProfitReceiver IDs.
func (prq *ProfitReceiverQuery) IDs(ctx context.Context) (ids []int, err error) {
	if prq.ctx.Unique == nil && prq.path != nil {
		prq.Unique(true)
	}
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryIDs)
	if err = prq.Select(profitreceiver.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (prq *ProfitReceiverQuery) IDsX(ctx context.Context) []int {
	ids, err := prq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (prq *ProfitReceiverQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryCount)
	if err := prq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, prq, querierCount[*ProfitReceiverQuery](), prq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (prq *ProfitReceiverQuery) CountX(ctx context.Context) int {
	count, err := prq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (prq *ProfitReceiverQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, prq.ctx, ent.OpQueryExist)
	switch _, err := prq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (prq *ProfitReceiverQuery) ExistX(ctx context.Context) bool {
	exist, err := prq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProfitReceiverQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (prq *ProfitReceiverQuery) Clone() *ProfitReceiverQuery {
	if prq == nil {
		return nil
	}
	return &ProfitReceiverQuery{
		config:                   prq.config,
		ctx:                      prq.ctx.Clone(),
		order:                    append([]profitreceiver.OrderOption{}, prq.order...),
		inters:                   append([]Interceptor{}, prq.inters...),
		predicates:               append([]predicate.ProfitReceiver{}, prq.predicates...),
		withBackgroundScenicArea: prq.withBackgroundScenicArea.Clone(),
		// clone intermediate query.
		sql:  prq.sql.Clone(),
		path: prq.path,
	}
}

// WithBackgroundScenicArea tells the query-builder to eager-load the nodes that are connected to
// the "background_scenic_area" edge. The optional arguments are used to configure the query builder of the edge.
func (prq *ProfitReceiverQuery) WithBackgroundScenicArea(opts ...func(*ScenicAreaQuery)) *ProfitReceiverQuery {
	query := (&ScenicAreaClient{config: prq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	prq.withBackgroundScenicArea = query
	return prq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProfitReceiver.Query().
//		GroupBy(profitreceiver.FieldScenicAreaID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (prq *ProfitReceiverQuery) GroupBy(field string, fields ...string) *ProfitReceiverGroupBy {
	prq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProfitReceiverGroupBy{build: prq}
	grbuild.flds = &prq.ctx.Fields
	grbuild.label = profitreceiver.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//	}
//
//	client.ProfitReceiver.Query().
//		Select(profitreceiver.FieldScenicAreaID).
//		Scan(ctx, &v)
func (prq *ProfitReceiverQuery) Select(fields ...string) *ProfitReceiverSelect {
	prq.ctx.Fields = append(prq.ctx.Fields, fields...)
	sbuild := &ProfitReceiverSelect{ProfitReceiverQuery: prq}
	sbuild.label = profitreceiver.Label
	sbuild.flds, sbuild.scan = &prq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProfitReceiverSelect configured with the given aggregations.
func (prq *ProfitReceiverQuery) Aggregate(fns ...AggregateFunc) *ProfitReceiverSelect {
	return prq.Select().Aggregate(fns...)
}

func (prq *ProfitReceiverQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range prq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, prq); err != nil {
				return err
			}
		}
	}
	for _, f := range prq.ctx.Fields {
		if !profitreceiver.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if prq.path != nil {
		prev, err := prq.path(ctx)
		if err != nil {
			return err
		}
		prq.sql = prev
	}
	return nil
}

func (prq *ProfitReceiverQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProfitReceiver, error) {
	var (
		nodes       = []*ProfitReceiver{}
		_spec       = prq.querySpec()
		loadedTypes = [1]bool{
			prq.withBackgroundScenicArea != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProfitReceiver).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProfitReceiver{config: prq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, prq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := prq.withBackgroundScenicArea; query != nil {
		if err := prq.loadBackgroundScenicArea(ctx, query, nodes, nil,
			func(n *ProfitReceiver, e *ScenicArea) { n.Edges.BackgroundScenicArea = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (prq *ProfitReceiverQuery) loadBackgroundScenicArea(ctx context.Context, query *ScenicAreaQuery, nodes []*ProfitReceiver, init func(*ProfitReceiver), assign func(*ProfitReceiver, *ScenicArea)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ProfitReceiver)
	for i := range nodes {
		fk := nodes[i].ScenicAreaID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(scenicarea.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "scenic_area_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (prq *ProfitReceiverQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := prq.querySpec()
	_spec.Node.Columns = prq.ctx.Fields
	if len(prq.ctx.Fields) > 0 {
		_spec.Unique = prq.ctx.Unique != nil && *prq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, prq.driver, _spec)
}

func (prq *ProfitReceiverQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(profitreceiver.Table, profitreceiver.Columns, sqlgraph.NewFieldSpec(profitreceiver.FieldID, field.TypeInt))
	_spec.From = prq.sql
	if unique := prq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if prq.path != nil {
		_spec.Unique = true
	}
	if fields := prq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, profitreceiver.FieldID)
		for i := range fields {
			if fields[i] != profitreceiver.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if prq.withBackgroundScenicArea != nil {
			_spec.Node.AddColumnOnce(profitreceiver.FieldScenicAreaID)
		}
	}
	if ps := prq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := prq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := prq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := prq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (prq *ProfitReceiverQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(prq.driver.Dialect())
	t1 := builder.Table(profitreceiver.Table)
	columns := prq.ctx.Fields
	if len(columns) == 0 {
		columns = profitreceiver.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if prq.sql != nil {
		selector = prq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if prq.ctx.Unique != nil && *prq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range prq.predicates {
		p(selector)
	}
	for _, p := range prq.order {
		p(selector)
	}
	if offset := prq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := prq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProfitReceiverGroupBy is the group-by builder for ProfitReceiver entities.
type ProfitReceiverGroupBy struct {
	selector
	build *ProfitReceiverQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (prgb *ProfitReceiverGroupBy) Aggregate(fns ...AggregateFunc) *ProfitReceiverGroupBy {
	prgb.fns = append(prgb.fns, fns...)
	return prgb
}

// Scan applies the selector query and scans the result into the given value.
func (prgb *ProfitReceiverGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prgb.build.ctx, ent.OpQueryGroupBy)
	if err := prgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProfitReceiverQuery, *ProfitReceiverGroupBy](ctx, prgb.build, prgb, prgb.build.inters, v)
}

func (prgb *ProfitReceiverGroupBy) sqlScan(ctx context.Context, root *ProfitReceiverQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(prgb.fns))
	for _, fn := range prgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*prgb.flds)+len(prgb.fns))
		for _, f := range *prgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*prgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProfitReceiverSelect is the builder for selecting fields of ProfitReceiver entities.
type ProfitReceiverSelect struct {
	*ProfitReceiverQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (prs *ProfitReceiverSelect) Aggregate(fns ...AggregateFunc) *ProfitReceiverSelect {
	prs.fns = append(prs.fns, fns...)
	return prs
}

// Scan applies the selector query and scans the result into the given value.
func (prs *ProfitReceiverSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prs.ctx, ent.OpQuerySelect)
	if err := prs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProfitReceiverQuery, *ProfitReceiverSelect](ctx, prs.ProfitReceiverQuery, prs, prs.inters, v)
}

func (prs *ProfitReceiverSelect) sqlScan(ctx context.Context, root *ProfitReceiverQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(prs.fns))
	for _, fn := range prs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*prs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
