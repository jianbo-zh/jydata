// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"github.com/jianbo-zh/jydata/database/ent/scenicarea"
	"github.com/jianbo-zh/jydata/database/ent/scenicareamap"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ScenicAreaMapQuery is the builder for querying ScenicAreaMap entities.
type ScenicAreaMapQuery struct {
	config
	ctx                      *QueryContext
	order                    []scenicareamap.OrderOption
	inters                   []Interceptor
	predicates               []predicate.ScenicAreaMap
	withBackgroundScenicArea *ScenicAreaQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ScenicAreaMapQuery builder.
func (samq *ScenicAreaMapQuery) Where(ps ...predicate.ScenicAreaMap) *ScenicAreaMapQuery {
	samq.predicates = append(samq.predicates, ps...)
	return samq
}

// Limit the number of records to be returned by this query.
func (samq *ScenicAreaMapQuery) Limit(limit int) *ScenicAreaMapQuery {
	samq.ctx.Limit = &limit
	return samq
}

// Offset to start from.
func (samq *ScenicAreaMapQuery) Offset(offset int) *ScenicAreaMapQuery {
	samq.ctx.Offset = &offset
	return samq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (samq *ScenicAreaMapQuery) Unique(unique bool) *ScenicAreaMapQuery {
	samq.ctx.Unique = &unique
	return samq
}

// Order specifies how the records should be ordered.
func (samq *ScenicAreaMapQuery) Order(o ...scenicareamap.OrderOption) *ScenicAreaMapQuery {
	samq.order = append(samq.order, o...)
	return samq
}

// QueryBackgroundScenicArea chains the current query on the "background_scenic_area" edge.
func (samq *ScenicAreaMapQuery) QueryBackgroundScenicArea() *ScenicAreaQuery {
	query := (&ScenicAreaClient{config: samq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := samq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := samq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(scenicareamap.Table, scenicareamap.FieldID, selector),
			sqlgraph.To(scenicarea.Table, scenicarea.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, scenicareamap.BackgroundScenicAreaTable, scenicareamap.BackgroundScenicAreaColumn),
		)
		fromU = sqlgraph.SetNeighbors(samq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ScenicAreaMap entity from the query.
// Returns a *NotFoundError when no ScenicAreaMap was found.
func (samq *ScenicAreaMapQuery) First(ctx context.Context) (*ScenicAreaMap, error) {
	nodes, err := samq.Limit(1).All(setContextOp(ctx, samq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{scenicareamap.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (samq *ScenicAreaMapQuery) FirstX(ctx context.Context) *ScenicAreaMap {
	node, err := samq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ScenicAreaMap ID from the query.
// Returns a *NotFoundError when no ScenicAreaMap ID was found.
func (samq *ScenicAreaMapQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = samq.Limit(1).IDs(setContextOp(ctx, samq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{scenicareamap.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (samq *ScenicAreaMapQuery) FirstIDX(ctx context.Context) int {
	id, err := samq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ScenicAreaMap entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ScenicAreaMap entity is found.
// Returns a *NotFoundError when no ScenicAreaMap entities are found.
func (samq *ScenicAreaMapQuery) Only(ctx context.Context) (*ScenicAreaMap, error) {
	nodes, err := samq.Limit(2).All(setContextOp(ctx, samq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{scenicareamap.Label}
	default:
		return nil, &NotSingularError{scenicareamap.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (samq *ScenicAreaMapQuery) OnlyX(ctx context.Context) *ScenicAreaMap {
	node, err := samq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ScenicAreaMap ID in the query.
// Returns a *NotSingularError when more than one ScenicAreaMap ID is found.
// Returns a *NotFoundError when no entities are found.
func (samq *ScenicAreaMapQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = samq.Limit(2).IDs(setContextOp(ctx, samq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{scenicareamap.Label}
	default:
		err = &NotSingularError{scenicareamap.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (samq *ScenicAreaMapQuery) OnlyIDX(ctx context.Context) int {
	id, err := samq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ScenicAreaMaps.
func (samq *ScenicAreaMapQuery) All(ctx context.Context) ([]*ScenicAreaMap, error) {
	ctx = setContextOp(ctx, samq.ctx, ent.OpQueryAll)
	if err := samq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ScenicAreaMap, *ScenicAreaMapQuery]()
	return withInterceptors[[]*ScenicAreaMap](ctx, samq, qr, samq.inters)
}

// AllX is like All, but panics if an error occurs.
func (samq *ScenicAreaMapQuery) AllX(ctx context.Context) []*ScenicAreaMap {
	nodes, err := samq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ScenicAreaMap IDs.
func (samq *ScenicAreaMapQuery) IDs(ctx context.Context) (ids []int, err error) {
	if samq.ctx.Unique == nil && samq.path != nil {
		samq.Unique(true)
	}
	ctx = setContextOp(ctx, samq.ctx, ent.OpQueryIDs)
	if err = samq.Select(scenicareamap.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (samq *ScenicAreaMapQuery) IDsX(ctx context.Context) []int {
	ids, err := samq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (samq *ScenicAreaMapQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, samq.ctx, ent.OpQueryCount)
	if err := samq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, samq, querierCount[*ScenicAreaMapQuery](), samq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (samq *ScenicAreaMapQuery) CountX(ctx context.Context) int {
	count, err := samq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (samq *ScenicAreaMapQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, samq.ctx, ent.OpQueryExist)
	switch _, err := samq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (samq *ScenicAreaMapQuery) ExistX(ctx context.Context) bool {
	exist, err := samq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ScenicAreaMapQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (samq *ScenicAreaMapQuery) Clone() *ScenicAreaMapQuery {
	if samq == nil {
		return nil
	}
	return &ScenicAreaMapQuery{
		config:                   samq.config,
		ctx:                      samq.ctx.Clone(),
		order:                    append([]scenicareamap.OrderOption{}, samq.order...),
		inters:                   append([]Interceptor{}, samq.inters...),
		predicates:               append([]predicate.ScenicAreaMap{}, samq.predicates...),
		withBackgroundScenicArea: samq.withBackgroundScenicArea.Clone(),
		// clone intermediate query.
		sql:  samq.sql.Clone(),
		path: samq.path,
	}
}

// WithBackgroundScenicArea tells the query-builder to eager-load the nodes that are connected to
// the "background_scenic_area" edge. The optional arguments are used to configure the query builder of the edge.
func (samq *ScenicAreaMapQuery) WithBackgroundScenicArea(opts ...func(*ScenicAreaQuery)) *ScenicAreaMapQuery {
	query := (&ScenicAreaClient{config: samq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	samq.withBackgroundScenicArea = query
	return samq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ScenicAreaMap.Query().
//		GroupBy(scenicareamap.FieldScenicAreaID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (samq *ScenicAreaMapQuery) GroupBy(field string, fields ...string) *ScenicAreaMapGroupBy {
	samq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ScenicAreaMapGroupBy{build: samq}
	grbuild.flds = &samq.ctx.Fields
	grbuild.label = scenicareamap.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ScenicAreaID int `json:"scenic_area_id,omitempty"`
//	}
//
//	client.ScenicAreaMap.Query().
//		Select(scenicareamap.FieldScenicAreaID).
//		Scan(ctx, &v)
func (samq *ScenicAreaMapQuery) Select(fields ...string) *ScenicAreaMapSelect {
	samq.ctx.Fields = append(samq.ctx.Fields, fields...)
	sbuild := &ScenicAreaMapSelect{ScenicAreaMapQuery: samq}
	sbuild.label = scenicareamap.Label
	sbuild.flds, sbuild.scan = &samq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ScenicAreaMapSelect configured with the given aggregations.
func (samq *ScenicAreaMapQuery) Aggregate(fns ...AggregateFunc) *ScenicAreaMapSelect {
	return samq.Select().Aggregate(fns...)
}

func (samq *ScenicAreaMapQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range samq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, samq); err != nil {
				return err
			}
		}
	}
	for _, f := range samq.ctx.Fields {
		if !scenicareamap.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if samq.path != nil {
		prev, err := samq.path(ctx)
		if err != nil {
			return err
		}
		samq.sql = prev
	}
	return nil
}

func (samq *ScenicAreaMapQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ScenicAreaMap, error) {
	var (
		nodes       = []*ScenicAreaMap{}
		_spec       = samq.querySpec()
		loadedTypes = [1]bool{
			samq.withBackgroundScenicArea != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ScenicAreaMap).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ScenicAreaMap{config: samq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, samq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := samq.withBackgroundScenicArea; query != nil {
		if err := samq.loadBackgroundScenicArea(ctx, query, nodes, nil,
			func(n *ScenicAreaMap, e *ScenicArea) { n.Edges.BackgroundScenicArea = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (samq *ScenicAreaMapQuery) loadBackgroundScenicArea(ctx context.Context, query *ScenicAreaQuery, nodes []*ScenicAreaMap, init func(*ScenicAreaMap), assign func(*ScenicAreaMap, *ScenicArea)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*ScenicAreaMap)
	for i := range nodes {
		fk := nodes[i].ScenicAreaID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(scenicarea.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "scenic_area_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (samq *ScenicAreaMapQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := samq.querySpec()
	_spec.Node.Columns = samq.ctx.Fields
	if len(samq.ctx.Fields) > 0 {
		_spec.Unique = samq.ctx.Unique != nil && *samq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, samq.driver, _spec)
}

func (samq *ScenicAreaMapQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(scenicareamap.Table, scenicareamap.Columns, sqlgraph.NewFieldSpec(scenicareamap.FieldID, field.TypeInt))
	_spec.From = samq.sql
	if unique := samq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if samq.path != nil {
		_spec.Unique = true
	}
	if fields := samq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, scenicareamap.FieldID)
		for i := range fields {
			if fields[i] != scenicareamap.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if samq.withBackgroundScenicArea != nil {
			_spec.Node.AddColumnOnce(scenicareamap.FieldScenicAreaID)
		}
	}
	if ps := samq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := samq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := samq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := samq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (samq *ScenicAreaMapQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(samq.driver.Dialect())
	t1 := builder.Table(scenicareamap.Table)
	columns := samq.ctx.Fields
	if len(columns) == 0 {
		columns = scenicareamap.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if samq.sql != nil {
		selector = samq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if samq.ctx.Unique != nil && *samq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range samq.predicates {
		p(selector)
	}
	for _, p := range samq.order {
		p(selector)
	}
	if offset := samq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := samq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ScenicAreaMapGroupBy is the group-by builder for ScenicAreaMap entities.
type ScenicAreaMapGroupBy struct {
	selector
	build *ScenicAreaMapQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (samgb *ScenicAreaMapGroupBy) Aggregate(fns ...AggregateFunc) *ScenicAreaMapGroupBy {
	samgb.fns = append(samgb.fns, fns...)
	return samgb
}

// Scan applies the selector query and scans the result into the given value.
func (samgb *ScenicAreaMapGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, samgb.build.ctx, ent.OpQueryGroupBy)
	if err := samgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ScenicAreaMapQuery, *ScenicAreaMapGroupBy](ctx, samgb.build, samgb, samgb.build.inters, v)
}

func (samgb *ScenicAreaMapGroupBy) sqlScan(ctx context.Context, root *ScenicAreaMapQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(samgb.fns))
	for _, fn := range samgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*samgb.flds)+len(samgb.fns))
		for _, f := range *samgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*samgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := samgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ScenicAreaMapSelect is the builder for selecting fields of ScenicAreaMap entities.
type ScenicAreaMapSelect struct {
	*ScenicAreaMapQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sams *ScenicAreaMapSelect) Aggregate(fns ...AggregateFunc) *ScenicAreaMapSelect {
	sams.fns = append(sams.fns, fns...)
	return sams
}

// Scan applies the selector query and scans the result into the given value.
func (sams *ScenicAreaMapSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sams.ctx, ent.OpQuerySelect)
	if err := sams.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ScenicAreaMapQuery, *ScenicAreaMapSelect](ctx, sams.ScenicAreaMapQuery, sams, sams.inters, v)
}

func (sams *ScenicAreaMapSelect) sqlScan(ctx context.Context, root *ScenicAreaMapQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sams.fns))
	for _, fn := range sams.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sams.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sams.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
