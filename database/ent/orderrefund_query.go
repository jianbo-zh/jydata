// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"github.com/jianbo-zh/jydata/database/ent/order"
	"github.com/jianbo-zh/jydata/database/ent/orderrefund"
	"github.com/jianbo-zh/jydata/database/ent/predicate"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// OrderRefundQuery is the builder for querying OrderRefund entities.
type OrderRefundQuery struct {
	config
	ctx        *QueryContext
	order      []orderrefund.OrderOption
	inters     []Interceptor
	predicates []predicate.OrderRefund
	withOrder  *OrderQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the OrderRefundQuery builder.
func (orq *OrderRefundQuery) Where(ps ...predicate.OrderRefund) *OrderRefundQuery {
	orq.predicates = append(orq.predicates, ps...)
	return orq
}

// Limit the number of records to be returned by this query.
func (orq *OrderRefundQuery) Limit(limit int) *OrderRefundQuery {
	orq.ctx.Limit = &limit
	return orq
}

// Offset to start from.
func (orq *OrderRefundQuery) Offset(offset int) *OrderRefundQuery {
	orq.ctx.Offset = &offset
	return orq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (orq *OrderRefundQuery) Unique(unique bool) *OrderRefundQuery {
	orq.ctx.Unique = &unique
	return orq
}

// Order specifies how the records should be ordered.
func (orq *OrderRefundQuery) Order(o ...orderrefund.OrderOption) *OrderRefundQuery {
	orq.order = append(orq.order, o...)
	return orq
}

// QueryOrder chains the current query on the "order" edge.
func (orq *OrderRefundQuery) QueryOrder() *OrderQuery {
	query := (&OrderClient{config: orq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := orq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := orq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(orderrefund.Table, orderrefund.FieldID, selector),
			sqlgraph.To(order.Table, order.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, orderrefund.OrderTable, orderrefund.OrderColumn),
		)
		fromU = sqlgraph.SetNeighbors(orq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first OrderRefund entity from the query.
// Returns a *NotFoundError when no OrderRefund was found.
func (orq *OrderRefundQuery) First(ctx context.Context) (*OrderRefund, error) {
	nodes, err := orq.Limit(1).All(setContextOp(ctx, orq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{orderrefund.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (orq *OrderRefundQuery) FirstX(ctx context.Context) *OrderRefund {
	node, err := orq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first OrderRefund ID from the query.
// Returns a *NotFoundError when no OrderRefund ID was found.
func (orq *OrderRefundQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = orq.Limit(1).IDs(setContextOp(ctx, orq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{orderrefund.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (orq *OrderRefundQuery) FirstIDX(ctx context.Context) int {
	id, err := orq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single OrderRefund entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one OrderRefund entity is found.
// Returns a *NotFoundError when no OrderRefund entities are found.
func (orq *OrderRefundQuery) Only(ctx context.Context) (*OrderRefund, error) {
	nodes, err := orq.Limit(2).All(setContextOp(ctx, orq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{orderrefund.Label}
	default:
		return nil, &NotSingularError{orderrefund.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (orq *OrderRefundQuery) OnlyX(ctx context.Context) *OrderRefund {
	node, err := orq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only OrderRefund ID in the query.
// Returns a *NotSingularError when more than one OrderRefund ID is found.
// Returns a *NotFoundError when no entities are found.
func (orq *OrderRefundQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = orq.Limit(2).IDs(setContextOp(ctx, orq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{orderrefund.Label}
	default:
		err = &NotSingularError{orderrefund.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (orq *OrderRefundQuery) OnlyIDX(ctx context.Context) int {
	id, err := orq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of OrderRefunds.
func (orq *OrderRefundQuery) All(ctx context.Context) ([]*OrderRefund, error) {
	ctx = setContextOp(ctx, orq.ctx, ent.OpQueryAll)
	if err := orq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*OrderRefund, *OrderRefundQuery]()
	return withInterceptors[[]*OrderRefund](ctx, orq, qr, orq.inters)
}

// AllX is like All, but panics if an error occurs.
func (orq *OrderRefundQuery) AllX(ctx context.Context) []*OrderRefund {
	nodes, err := orq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of OrderRefund IDs.
func (orq *OrderRefundQuery) IDs(ctx context.Context) (ids []int, err error) {
	if orq.ctx.Unique == nil && orq.path != nil {
		orq.Unique(true)
	}
	ctx = setContextOp(ctx, orq.ctx, ent.OpQueryIDs)
	if err = orq.Select(orderrefund.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (orq *OrderRefundQuery) IDsX(ctx context.Context) []int {
	ids, err := orq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (orq *OrderRefundQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, orq.ctx, ent.OpQueryCount)
	if err := orq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, orq, querierCount[*OrderRefundQuery](), orq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (orq *OrderRefundQuery) CountX(ctx context.Context) int {
	count, err := orq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (orq *OrderRefundQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, orq.ctx, ent.OpQueryExist)
	switch _, err := orq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (orq *OrderRefundQuery) ExistX(ctx context.Context) bool {
	exist, err := orq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the OrderRefundQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (orq *OrderRefundQuery) Clone() *OrderRefundQuery {
	if orq == nil {
		return nil
	}
	return &OrderRefundQuery{
		config:     orq.config,
		ctx:        orq.ctx.Clone(),
		order:      append([]orderrefund.OrderOption{}, orq.order...),
		inters:     append([]Interceptor{}, orq.inters...),
		predicates: append([]predicate.OrderRefund{}, orq.predicates...),
		withOrder:  orq.withOrder.Clone(),
		// clone intermediate query.
		sql:  orq.sql.Clone(),
		path: orq.path,
	}
}

// WithOrder tells the query-builder to eager-load the nodes that are connected to
// the "order" edge. The optional arguments are used to configure the query builder of the edge.
func (orq *OrderRefundQuery) WithOrder(opts ...func(*OrderQuery)) *OrderRefundQuery {
	query := (&OrderClient{config: orq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	orq.withOrder = query
	return orq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Type int `json:"type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.OrderRefund.Query().
//		GroupBy(orderrefund.FieldType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (orq *OrderRefundQuery) GroupBy(field string, fields ...string) *OrderRefundGroupBy {
	orq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &OrderRefundGroupBy{build: orq}
	grbuild.flds = &orq.ctx.Fields
	grbuild.label = orderrefund.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Type int `json:"type,omitempty"`
//	}
//
//	client.OrderRefund.Query().
//		Select(orderrefund.FieldType).
//		Scan(ctx, &v)
func (orq *OrderRefundQuery) Select(fields ...string) *OrderRefundSelect {
	orq.ctx.Fields = append(orq.ctx.Fields, fields...)
	sbuild := &OrderRefundSelect{OrderRefundQuery: orq}
	sbuild.label = orderrefund.Label
	sbuild.flds, sbuild.scan = &orq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a OrderRefundSelect configured with the given aggregations.
func (orq *OrderRefundQuery) Aggregate(fns ...AggregateFunc) *OrderRefundSelect {
	return orq.Select().Aggregate(fns...)
}

func (orq *OrderRefundQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range orq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, orq); err != nil {
				return err
			}
		}
	}
	for _, f := range orq.ctx.Fields {
		if !orderrefund.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if orq.path != nil {
		prev, err := orq.path(ctx)
		if err != nil {
			return err
		}
		orq.sql = prev
	}
	return nil
}

func (orq *OrderRefundQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*OrderRefund, error) {
	var (
		nodes       = []*OrderRefund{}
		_spec       = orq.querySpec()
		loadedTypes = [1]bool{
			orq.withOrder != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*OrderRefund).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &OrderRefund{config: orq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, orq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := orq.withOrder; query != nil {
		if err := orq.loadOrder(ctx, query, nodes, nil,
			func(n *OrderRefund, e *Order) { n.Edges.Order = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (orq *OrderRefundQuery) loadOrder(ctx context.Context, query *OrderQuery, nodes []*OrderRefund, init func(*OrderRefund), assign func(*OrderRefund, *Order)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*OrderRefund)
	for i := range nodes {
		fk := nodes[i].OrderID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(order.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "order_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (orq *OrderRefundQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := orq.querySpec()
	_spec.Node.Columns = orq.ctx.Fields
	if len(orq.ctx.Fields) > 0 {
		_spec.Unique = orq.ctx.Unique != nil && *orq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, orq.driver, _spec)
}

func (orq *OrderRefundQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(orderrefund.Table, orderrefund.Columns, sqlgraph.NewFieldSpec(orderrefund.FieldID, field.TypeInt))
	_spec.From = orq.sql
	if unique := orq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if orq.path != nil {
		_spec.Unique = true
	}
	if fields := orq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, orderrefund.FieldID)
		for i := range fields {
			if fields[i] != orderrefund.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if orq.withOrder != nil {
			_spec.Node.AddColumnOnce(orderrefund.FieldOrderID)
		}
	}
	if ps := orq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := orq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := orq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := orq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (orq *OrderRefundQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(orq.driver.Dialect())
	t1 := builder.Table(orderrefund.Table)
	columns := orq.ctx.Fields
	if len(columns) == 0 {
		columns = orderrefund.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if orq.sql != nil {
		selector = orq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if orq.ctx.Unique != nil && *orq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range orq.predicates {
		p(selector)
	}
	for _, p := range orq.order {
		p(selector)
	}
	if offset := orq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := orq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// OrderRefundGroupBy is the group-by builder for OrderRefund entities.
type OrderRefundGroupBy struct {
	selector
	build *OrderRefundQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (orgb *OrderRefundGroupBy) Aggregate(fns ...AggregateFunc) *OrderRefundGroupBy {
	orgb.fns = append(orgb.fns, fns...)
	return orgb
}

// Scan applies the selector query and scans the result into the given value.
func (orgb *OrderRefundGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, orgb.build.ctx, ent.OpQueryGroupBy)
	if err := orgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderRefundQuery, *OrderRefundGroupBy](ctx, orgb.build, orgb, orgb.build.inters, v)
}

func (orgb *OrderRefundGroupBy) sqlScan(ctx context.Context, root *OrderRefundQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(orgb.fns))
	for _, fn := range orgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*orgb.flds)+len(orgb.fns))
		for _, f := range *orgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*orgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := orgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// OrderRefundSelect is the builder for selecting fields of OrderRefund entities.
type OrderRefundSelect struct {
	*OrderRefundQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ors *OrderRefundSelect) Aggregate(fns ...AggregateFunc) *OrderRefundSelect {
	ors.fns = append(ors.fns, fns...)
	return ors
}

// Scan applies the selector query and scans the result into the given value.
func (ors *OrderRefundSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ors.ctx, ent.OpQuerySelect)
	if err := ors.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*OrderRefundQuery, *OrderRefundSelect](ctx, ors.OrderRefundQuery, ors, ors.inters, v)
}

func (ors *OrderRefundSelect) sqlScan(ctx context.Context, root *OrderRefundQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ors.fns))
	for _, fn := range ors.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ors.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ors.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
